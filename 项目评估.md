# DirectorDesk 二开为商业网页端的项目评估

> **结论：有条件建议立项，先验证受限 AI 镜头设计，再投入公开收费版本。**
>
> 现有工程已经具备浏览器编辑与预演基础，网页化不需要从零开发三维引擎；但内置 AI 依赖桌面桥接，账户、服务端任务和计费需要新增。不能把“前端部署成功”当成“商业 SaaS 改造完成”。
>
> 评估日期：2026-09-13。源码基线：DirectorDesk 0.4.8 / Git `eee9234`。本报告基于静态代码核查及已有规划，未运行产品、自动测试、负载测试或真实模型付费测试。所有成功率、时延、费用和商业转化阈值均为拟验证目标或演算假设，不是当前实测。

## 一、评估范围与决策摘要

### 1.1 项目范围

拟开发产品：面向 AI 短剧与剧情短片创作者的网页镜头设计助手，允许通过文字创建一个可编辑三维镜头，并在保留原安排的前提下局部修改机位或人物走位，最终导出工程，以及对应单镜头的视频、首尾帧、通用提示词和素材清单。

首发为桌面浏览器，不包括最终风格化视频生成、云渲染、移动端完整编辑、多人协同和完整短剧自动生成。

文档与源码位置：

- 当前规划仓库：`wocaonima335/director-desk-web`，本地 `E:\myProgram\DirectorDesk\director-desk-web`。
- 源码仍在：`E:\myProgram\DirectorDesk\director-desk`；新仓库此前仅提交规划文档，尚未迁入产品源码。
- [产品与技术方案](plan.md)。
- [38 项 AI 开发任务](docs/ai-development-tasks.md)。
- [开发排期与门禁](docs/development-schedule.md)。
- [视频与提示词参考包规格](docs/video-reference-package.md)：REF-01，首发仅通用包，专用模型能力待核验。

### 1.2 综合判断

| 维度 | 判断 | 证据/限制 | 对决策的影响 |
|---|---|---|---|
| 网页技术可行性 | 较高 | 已有 Vite、WebGL 和浏览器导入导出路径 | 无需重写三维核心 |
| 简化交互可行性 | 较高 | 可在现有工具之上增加任务入口与参数套件 | 需要使用测试，不只是换颜色 |
| AI 镜头设计可行性 | 中等、有条件 | 有结构化编辑执行器，但网页 AI 服务和稳定性未验证 | 限制场景与指令，建立评测集 |
| 收费系统可行性 | 常规可实现，但缺少现成业务层 | 登录、账本、支付、退款和恢复均需建设 | 属于主要新增工程量 |
| 浏览器成品质量 | 待验证 | WebGL/编码/内存受设备影响 | 首发需支持矩阵与规格限制 |
| 独立产品付费意愿 | 未验证 | 不可用 AI 视频大市场替代本产品需求 | 先做付费试点，不预估收入增长 |
| 差异化持续性 | 中等偏弱，取决于执行 | 换皮和聊天入口易复制，工作流可靠性更有价值 | 护城河要建立在任务效果与复用上 |
| 许可可行性 | 有商业二开基础 | 自有代码 MIT，实际发行依赖和资产仍须核验 | 必须保留归属、使用自有品牌 |

不提供看似精确的总分或“成功概率”。当前缺少真实使用、费用、留存和市场转化数据，无法把这些维度压成可靠的投资评分。

### 1.3 推荐立项方式

批准一个分阶段、有限预算的开发项目，而不是一次性承诺全平台：

1. 验证浏览器基础和受约束 AI 创建/修改。
2. 验证新手能够完成任务以及失败不破坏工程。
3. 建立计费、恢复、授权和输出后，再开放限额收费。
4. 观察真实第二项目复用和付费后，再扩大模板、模型或团队功能。

## 二、现有源码到底能复用什么

以下路径相对于原始源码目录；新文档仓库目前没有这些源码文件。行号基于本次基线，后续变更会移动。

| 模块 | 源码证据 | 复用判断 | 必须补充或验证 |
|---|---|---|---|
| 浏览器入口 | `index.html:10`、`package.json:11-12` | 直接复用 Vite 入口和构建 | 资源路径、生产托管、实际构建 |
| 三维编辑与预演 | `src/engine.ts`、`src/main.ts` | 保留 Three.js/WebGL 和编辑核心 | 低配设备与复杂工程性能 |
| 场景、机位、动作 | `src/model.ts`、`src/scenes/` | 复用数据结构与现有操作 | AI使用范围、稳定ID与模板版本 |
| 网页 AI 面板 | `src/ui/ai-panel.ts:9-53` | 复用部分交互，不可直接上线 | 无桌面bridge时执行受阻，需网页transport |
| 桌面模型调用 | `desktop/ai-host.cjs:39-109`、`desktop/providers.cjs` | 参考请求适配和工具循环逻辑 | 去除Electron/文件系统/本机密钥依赖 |
| 结构化编辑 | `src/automation/service.ts:136-168` | 复用预检、revision与提交 | 持久恢复、保护范围、任务级事务 |
| 浏览器本地保存 | `src/storage.ts`、`src/ui/project-save.ts` | 保留IndexedDB与工程下载 | 品牌隔离、配额失败、多标签页与备份提示 |
| 视频导出 | `src/export.ts`、`src/exporting/delivery.ts` | 保留Mediabunny编码路径 | codec支持、下载模式、内存与取消 |
| 场景与模板复用 | `src/ui/scene-reuse-panel.ts` | 已有，不重新列作原创独有功能 | 参数化任务套件和首次使用引导 |
| 自动测试 | `package.json`、`tests/` | 复用Node测试与现有验证脚本 | 先执行基线；新增Web/服务端/账务测试 |

**不报告“可复用 80% 代码”之类数字。** 未做模块工时和耦合度量，代码量比例也不等于工作量比例。准确结论是：三维基础可以大量沿用，商业在线服务需要明显新增。

关键误区：

- `preview:true` 是编辑预检，不是候选镜头画面的完整视觉预览。
- 内存 requestId receipts 不等于持久幂等，不可用于支付结算。
- 本地自动恢复不等于云端备份。
- 视频导出开始不等于文件已成功存到客户硬盘。
- 模型能调用工具不等于能稳定设计合格镜头。

## 三、三条实现路线比较

| 路线 | 范围 | 优点 | 局限 | 建议 |
|---|---|---|---|---|
| A：静态部署与轻度换皮 | 现有编辑器网页化、改主题和导航 | 最快检查浏览器基础 | 网页内置AI仍缺失，付费理由弱 | 仅用作内部原型 |
| B：简化编辑器＋云AI＋有限付费 | 套件、创建/局部修改、账号额度、可靠输出 | 贴近当前目标，利用现有核心 | 需承担AI质量、服务与计费 | **推荐首发路线** |
| C：全云影视生产平台 | 云工程、渲染、生成视频、多用户协作 | 想象空间大 | 范围与运维成本显著增大 | 首发后置，另行估算 |

路线 B 的关键不是“让 Electron 在云端运行”，而是浏览器保留三维交互，Node 服务处理受控 AI 提案和业务状态。没有云渲染需求时，不必为了网页化先采购 GPU 服务器。

## 四、商业定位与收费依据

### 4.1 可售价值

推荐定位：**描述你想拍的镜头，AI 帮你安排机位和走位；不满意可以局部修改。**

三个可验证卖点：

1. 降低起步成本：从空白三维场景转为选任务、说需求、看预演。
2. 提高修改效率：保持场景和人物不变，只调整指定摄影机或选定对象。
3. 输出可编辑结果与对应资料：工程可继续修改，导出的视频和通用提示词绑定同一范围与版本，降低后续生成输入整理成本。

网页本身提供免安装与更短试用路径，但必须用真实浏览器体验验证，不能假设“免安装必然高转化”。

### 4.2 为什么会选择免费上游之外的版本

付费应对应：无需自行配置模型、成熟镜头套件、稳定自然语言修改、清楚的任务状态、可恢复执行与可靠支持。基础三维功能来自上游，必须透明说明。

如果只是主题修改和在右侧加聊天框，差异化不足；如果客户仍要反复研究关键帧和坐标才能修好 AI 的错误，则“降低门槛”没有兑现。

### 4.3 目标客户与边界

首批面向已有项目的 AI 剧情创作者，而非全部视频用户。优先任务是双人对话、人物进出、跟拍、推进/环绕和简单对峙。

不把所有运镜相关搜索都视作付费潜客；很多简单镜头用模型内置控制或提示词即可解决。复杂调度可能有价值，但仍需比较预演增加的时间与减少的返工。

## 五、技术难点与实际工作量来源

### 5.1 AI 服务迁移

桌面端可以在单用户进程中调用模型和工具，云端需同时处理身份、预算、并发、隔离、队列、超时和重启。不能把桌面 AI handler 原样放进 API 路由。

推荐：规范化快照 → 有界语义规划 → 模板/操作编译 → 服务端schema与范围验证 → 持久提案 → 浏览器预检与提交。

首发限制为当前场景、一个摄影机、最多两个人物、5—15秒；模型调用最多三次，限制输入、操作数和总时间。达到上限返回可恢复错误，不提供无限Agent循环。

### 5.2 局部修改正确性

“不要改人物”必须变成可验证的字段保护，不依赖一句system prompt。比较操作前后工程，保护位置、路径、动作、锁定对象及非目标镜头。

语法通过与艺术效果分开检查：模型可生成合法参数却让人不在画面内、被墙遮挡或镜头方向错误；因此需要实际播放评审和固定任务集。

### 5.3 任务恢复与浏览器可信边界

浏览器工程是实际编辑状态，服务器只能获得提交时的快照。服务器不得假设自己持有最新场景；浏览器需对比revision和哈希后再应用。

刷新可能发生在服务端完成、浏览器收到、预检结束、工程提交的任一点。必须记录proposalId和前后状态，恢复时不盲目重复应用；有冲突默认保留现有工程，并提供在副本应用。

客户端可报告应用失败，但不能以此无条件无限返还额度。服务器账务以可信持久状态为准，真实产品错误通过可核验补偿处理。

### 5.4 网页渲染、资产与视频编码

WebGL和视频编码依赖设备、浏览器、驱动和资源规模。初期测试720p/24fps/5—15秒，并提供MP4不可用时的WebM回退；不承诺任意4K长视频。

首版使用少量内置许可清晰资源，限制超大导入。CDN/子路径资源需要验收，跨源资产不能造成截图或导出失败。

网页会分发前端代码与部分资源，不能把核心密钥或机密商业逻辑放入前端，也不能假设客户端功能可以靠混淆获得强保护。收费和用量控制必须在服务端。

### 5.5 保存、账号与云服务预期

首版登录仅关联身份和权益，工程默认当前浏览器保存。退出/换账号时隔离本地项目展示；浏览器清理会导致本地数据丢失，需突出备份和下载。

如果客户明确要求跨设备继续编辑，应新增云快照项目并重排工期，而不是静默扩展MVP或继续误导“登录即同步”。

## 六、推荐架构及首发缺口

```text
浏览器
  简单模式 / 套件 / 本地工程
  Three.js / 操作执行器 / 预演 / 编码导出
                 │ 规范化场景快照与任务
                 ▼
同源 API：Node.js + TypeScript
  认证 / 限额 / 持久任务 / Provider / 提案验证
  支付订单 / 额度账本 / 成本日志
                 │
                 ▼
PostgreSQL：任务、租约、提案、权益、账务
                 │
                 ▼
获批模型服务、邮件服务、支付服务
```

PostgreSQL持久队列是初期方案，不需要先引入完整消息中间件。Node API建议独立目录、共享纯DTO，不导入依赖AppContext的完整渲染执行器。

| 工作包 | 现状缺口 | 对应任务 |
|---|---|---|
| 网页与简单交互 | AI bridge、入口与参数负担 | WEB-003—006、009、014 |
| AI任务与约束 | 服务端调用、持久状态、scope与修复 | WEB-010—017 |
| 商业基础 | 登录、权益、订单、补偿与限额 | WEB-007—008、018—020、022 |
| 输出与可靠性 | 浏览器规格、刷新恢复、成本可观测 | WEB-016、021、023 |
| 发布准备 | 质量集、授权/账务、内容、许可、运维 | WEB-024—032 |
| 视频提示词参考包 | 范围事实、成片设定、首尾帧、同步和打包 | WEB-033—038 |

## 七、投入与排期评估

### 7.1 人力和日历

与现有排期一致：

- REF-01新增6项/9人日，当前38项任务、75开发人日：A37、B38；原32项/66人日保留为变更前基线。
- 两名开发者：9周基础窗口，90开发人日容量；15人日用于评审、等待与集成余量。
- 再预留2周风险缓冲，最多20开发人日容量；不是默认新增功能预算。
- 产品/测试支持约每周2天，9周合计约18人日，不包含在75开发人日中。
- 单人实施：建议17—20周，外部审批和重大技术问题另计。
- 首次限额付费获准后，再观察14天商业行为；不把这一观察期算成W9当天完成。

1人日按约6小时有效工程工作。估算已经考虑AI辅助编码，不应再次把周期按“AI会自动开发”减半。

### 7.2 一次性投入的预算模型

没有采购报价或实际薪资信息，以下仅为可替换的预算演算。假设开发者完全成本为800—1,500元/人日，产品/测试为600—1,000元/人日。

| 预算口径 | 开发部分 | 产品/测试部分 | 合计 |
|---|---:|---:|---:|
| 纯任务估算：75开发日+18支持日 | 60,000—112,500元 | 10,800—18,000元 | 70,800—130,500元 |
| 9周实际占用：90开发日+18支持日 | 72,000—135,000元 | 10,800—18,000元 | 82,800—153,000元 |
| 11周全部占用：110开发日+22支持日 | 88,000—165,000元 | 13,200—22,000元 | 101,200—187,000元 |

这不是外包报价，不包含主体/支付/法律咨询、模型评测、域名与托管、额外招聘、广告、税费或外部审批成本。自己开发也有时间机会成本，但不应将机会成本全部当作当期现金支出。

### 7.3 经常性支出构成

- 模型调用：与有效任务数、上下文长度、重试和模型版本相关。
- API和数据库：实例、磁盘、备份、日志与流量；并发验证后再定配置。
- 静态资源：首次模型/场景下载、CDN请求与带宽；浏览器本地视频导出不等于云端视频渲染成本。
- 邮件、支付手续费、退款/争议和客户支持。
- 持续评测与适配：provider变更、浏览器编码变化、上游合并。

在并发、资源体积和供应商未确定前，不提供“每月几十元即可稳定运营”等结论。

## 八、计费与单位经济性

### 8.1 首版收费设计

沿用plan：一次性购买30天AI额度包，不自动续费；手动编辑不扣AI额度。新增通用参考包由本地规则生成文稿和打包，重复下载不扣额度、不自动调用模型；AI文案润色后置。报价可测试49—99元，但正式单价与额度必须在模型基准后批准。

收费对象是已交付、可重取的有效结构化镜头提案，不是最终AI影片，也不是底层每个HTTP请求。预售页明确浏览器支持、结果定义和售后重试规则。

额度流程：服务端预留 → 生成并验证 → 提案与结算事务落库；失败/及时取消释放。前端断线可重新取回，不重复扣费；取消与成功竞态按持久终态决定。有效提案质量不佳与技术失败分别处理，禁止主观失败反馈导致无限模型调用。

### 8.2 成本演算示例

仅为敏感性分析，假设月度额度包79元、平均每付费账号每期交付80个提案；每有效提案成本包含成功和失败/重试的分摊。

| 假设每有效提案成本 | 每账号模型成本 | 收入减模型成本 | 尚未扣除 |
|---|---:|---:|---|
| 0.05元 | 4元 | 75元 | 支付、托管、退款、支持、获客、税费 |
| 0.30元 | 24元 | 55元 | 同上 |
| 1.00元 | 80元 | -1元 | 同上，模型层已亏损 |

这些不是实际token价格或模型测量结果，也不代表79元/80次应直接成为上线套餐。说明的是：结果成本和重度使用会改变商品可行性，不能先定无限量再寻找成本模型。

### 8.3 盈亏和获客

月度贡献 = 实收收入 − 模型成本 − 可变基础服务成本 − 支付/退款/支持等可变成本。

月度运营盈亏平衡付费人数 = 月固定运营成本 / 每付费账号平均月度贡献；只有贡献为正时成立。若假设固定成本20,000元、每账号贡献40元，则需要500个稳定付费账号，仅覆盖该固定运营口径，不代表收回开发投资；以上仍为演算假设。

一次性30天购买不是已验证的经常性收入，不能凭销售额乘12宣称订阅ARR。没有复购/留存前不能可靠估算LTV，也不宜据此确定大规模广告CAC预算。

## 九、风险评估与应对

风险等级是工程判断，不是统计概率。

| 风险 | 级别 | 早期信号 | 应对/发布影响 |
|---|---|---|---|
| AI生成合法但镜头不可用 | 高 | 人物出画、走位不符、机位方向错误 | 模板约束、画面评测；质量未达不收费发布 |
| 局部修改破坏无关内容 | 高 | camera-only修改人物/场景 | 前后字段保护、事务提交、一次撤销；零容忍 |
| 重复扣费或余额透支 | 高 | 回调/取消/重试出现多次账务事件 | 唯一事件、事务与并发测试；阻塞上线 |
| 公开接口被滥用耗费 | 高 | 免费账号异常并发、长上下文与重试 | 身份、额度、预算上限、限流与熔断 |
| 工程丢失/重复应用 | 高 | 刷新后重复对象、覆盖新修改 | 本地快照、proposal去重、冲突保留与备份 |
| 支付/模型地区条件不满足 | 高 | 开户失败、条款不允许目标销售地区 | W1前置核查；不开真实收费 |
| 提示词与视频错版 | 高 | 改机位后仍导出旧文稿、首尾帧取错 | 冻结同快照、相对时间映射、过期确认和20项REF验收 |
| 目标平台能力被误报 | 中高 | 默认MiniMax或SD2.0可传视频 | 仅通用适配，具体模型/入口核验后才宣传 |
| 视频导出不稳定 | 中高 | codec缺失、浏览器崩溃、内存耗尽 | 规格限制、能力探测、WebM回退 |
| 换皮不足以收费 | 高 | 试一次就走，免费上游完全替代 | 验证AI修改与上手效率，不靠遮蔽归属 |
| 平台内置功能替代 | 中高 | 简单镜头无需预演、客户不愿加步骤 | 聚焦复杂任务、保留模型无关文件输出 |
| 素材/品牌许可问题 | 中高 | 无授权资产、误用上游标识 | 来源清单、保留许可、自有品牌 |
| 二开维护成本膨胀 | 中 | 核心文件大量交叉改动、上游难合并 | 适配层、冻结基线、测试与小步合并 |
| 宣传与交付不一致 | 中高 | 客户以为买的是成片生成/云保存 | 首屏、价格、操作界面重复明确边界 |

MIT允许商业使用不代表资产和商标无条件可用。网页公开分发资源、第三方模型的数据条款和销售地区义务需分别核对；本报告不是法律合规认证。

## 十、验证路线与继续/停止条件

### 10.1 技术验证

| 阶段 | 需要的证据 | 继续条件 |
|---|---|---|
| 基线验证 | 原始工程构建、测试、浏览器启动记录 | 可复现环境与已知失败分离 |
| 受约束AI | 五类模板、创建与局部修改实际预演 | 不是纯文本建议，工程可编辑 |
| 故障一致性 | 超时、取消、刷新、重放、并发与支付事件 | 不丢工程、不越权、不重复收费 |
| 参考包一致性 | 20项REF用例、真实解压播放与hash复算 | 范围、帧、提示词同版，未知模型不误报 |
| 兼容性 | Windows Chrome/Edge、macOS Chrome实际版本 | 约定规格预演和导出通过 |
| 发布准备 | 预算、开户、许可、隐私、回退和支持 | 人审核准，关键阻塞为零 |

评测按现有plan：50条固定用例（20创建、20修改、10无效/恢复）和10条保留用例。90%成功率针对有效创建/修改请求，以预设画面与工程标准判定；无效请求应正确拒绝或恢复，不能以失败请求不生成提案拉低或混淆统计。保护字段破坏率必须为0，关键资金/授权用例全通过。

### 10.2 操作难度验证

5名目标创作者中至少4名在无开发者代操作下完成创建、一次局部修改、工程备份；目标中位用时小于10分钟。与原版执行同任务对比，固定任务、设备与引导条件，并记录顺序学习效应。

如果只能由开发者演示成功，而新手需要理解大量原版参数，优先修交互和模板，不继续扩大AI任务范围。

### 10.3 商业验证

建议招募10名具备真实项目的创作者，在限额付费试点后观察14天；内部目标为至少3人真实付费、其中至少2人用于第二个项目。小样本只用于下一轮决策，不代表市场转化率或统计显著性。

同步采集：每次有效任务成本、修改次数、失败与退款、支持时间、导出后是否进入实际制作。若客户不愿付费，先区分价格、效果、任务不适用、操作门槛，不直接归因于流量不足。

### 10.4 暂停条件

- 约定修复周期内，保护字段仍被破坏或恢复仍会丢工程。
- 关键资金用例不通过，或运营/支付条件未满足。
- 可接受质量下的任务成本无法支撑合理套餐，且限额/模型选择无法改善。
- 两轮迭代与目标客户复访后，仍无真实项目复用，新增流量仅带来体验者。

暂停公开收费不必删除工程；可以保留受控原型或转为模板/教学工具，但新方向必须重新验证。

## 十一、开发仓库与实施准备

新仓库目前是文档起点，不是可直接运行的产品。源码迁入应在WEB-001内先确认迁移方式：保留上游历史并添加upstream，或导入指定版本快照并记录来源。两者都应保留许可与基线；未经确认不改现有仓库历史或强推。

迁入后验证：依赖锁文件、资源、脚本、测试、LICENSE/NOTICE是否完整；新仓库文档不被原上游宽泛ignore规则意外排除；README说明商业增量与来源；使用独立产品数据和更新配置。

下列工作尚未完成，不能误认为已具备：

- 原始源码迁入新仓库；
- Node服务、数据库、生产域名及部署；
- 模型/邮件/支付供应商和正式品牌批准；
- 实际运行和浏览器验收；
- AI费用基准与产品定价；
- 真实客户付费与复用验证。

## 十二、最终建议

**建议选择路线B，有条件投入，按阶段释放预算。**

首版聚焦五类单镜头任务、自然语言局部修改、可靠撤销与恢复、通用视频提示词参考包和有限额度购买。当前75人日已包含REF-01净增9人日；不要把云渲染、多人协作、全剧生成或专用平台验证混入此估算。

决定项目成败的不是能否把编辑器放到网页，而是以下四点能否同时成立：

1. 不熟悉三维操作的客户也能完成一个可用镜头。
2. AI修改能保护已有工作，而不是每次重做。
3. 每次有效结果的成本可控，付费与恢复状态可信。
4. 客户会在第二个真实项目继续使用，并愿意为这些增量付费。

在这些条件得到证据支持前，将项目称为“商业网页端开发与验证计划”，而不是已验证的高增长SaaS业务。

## 十三、REF-01参考包增量评估

新增功能利用原项目逐场提示词、素材ZIP和视频导出基础，但新增价值是“实际片段与文稿一致”，不是宣称原版没有提示词。默认从当前工程提取机位/路径事实，不重新上传视频给多模态模型识别。

该功能技术风险主要在时间裁剪、采样末帧、复杂动作语义、人工改写与缓存失效。首发只做中文规则文稿、内置资源和单连续镜头；未知外观/动作不编造，人工改写需要确认。静态文件提取不产生模型用量，但会增加浏览器内存、ZIP与兼容测试负担。

商业上可以加强“下一步如何生成成片”的交付说明，但不能保证下游视频模型的遵循效果。MiniMax须核实具体模型与网页/API入口；SD 2.0指代未确认，不能自动当作Seedance 2.0，更不能把Stable Diffusion 2.0当作视频参考模型。

首发验收不要求已集成某个外部视频平台；专用适配、真实成片对照与AI文稿润色仍需独立预算。判断参考包价值，应观察客户是否实际使用、是否减少资料整理/错版，而不是仅统计下载次数。
