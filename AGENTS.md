# DirectorDesk 开发代理工作流

## 适用范围

本文件规定本仓库的二次开发代理调度方式。详细说明见 `.zcode/workflows/README.md`，任务模板见 `.zcode/workflows/task-template.yaml`。

当前仓库以规划文档为主。开始功能开发前，必须核实真实源码所在目录、Git 基线、已有修改和实际构建/测试入口；不得将规划文档中的目录当作已实现模块。

## 角色与路由

- 规划：通过 Agent 的 `subagent_type: director-planner` 调用 GPT。
- 编码：通过 `subagent_type: director-coder` 调用 GLM。
- 审查：通过 `subagent_type: director-reviewer` 调用 GPT。
- 主会话负责搜索准备、任务状态、取得计划批准、交接、返工计数和汇总，不绕过编码代理直接完成业务实现。
- 这是本轮批准的新分工：GPT 规划与审查，GLM 承担批准的编码任务。旧文档中 G/L 分模块主写的安排不用于本工作流；原有技术架构边界、契约冻结和人工审批要求继续适用。
- 模型绑定在代理定义的 model 字段中；提示词不能切换真实模型。代理不可用或模型失败时报告阻塞，不静默改用其他代理或继承模型。

## 调度流程

1. 记录需求、源码根目录、Git 基线及工作树初始状态；为 planner 提供相关文件清单和必要搜索证据。
2. planner 读取源码并给出 PLAN_READY 或 BLOCKED。PLAN_READY 后主会话取得人工计划批准；已有明确批准的计划无需重复询问。
3. 用任务模板记录批准范围与验收标准，再调用 coder。每次一个任务，同一工作树默认串行编码。
4. coder 返回 IMPLEMENTED 后调用独立 reviewer，提供任务、初始状态、实际差异范围及验证记录。
5. PASS 后汇总交付；REWORK 交回 coder；同一任务连续两轮返工仍未通过则转 planner 重规划。REPLAN 必须重新确认变更范围。
6. 关键检查未验证不得标为完成。模型互审不替代人工真实运行验收。

## 变更边界

- 保留已有未提交修改、删除与未跟踪文件。不要恢复、清理或覆盖无关改动。
- 扩大修改范围、新增依赖、修改公共契约和数据迁移前需获得批准。
- 并行写入必须使用独立工作树，并明确单一整合负责人。
- 未明确授权，不 commit、push、发布或使用真实付费业务资源。
- 开发代理模型与本机 provider 标识不是产品运行时配置，不能写入 BYOK 或业务逻辑。
- 子代理不得递归启动本流程；执行自己对应的单一角色即可。
