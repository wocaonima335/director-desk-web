# 桌面一期：10天AI快速开发任务列表

> DESKTOP-04。项目负责人：**xueyangfei**。规划/代码审查：**gpt-6-astra**；编码由配置的 **director-coder** 执行。DSK-001～003 技术审查通过（人工待验收）；DSK-004 SQLite 门禁独立 PASS，产品首轮审查 REWORK，返工 A 段已实施并由 coder 验证，B 段未完成、整体未复审。DSK-005～008及016未开始，N1保留为008前契约门禁。按最新指令暂停并保存GitHub检查点，详见[进度.md](进度.md)。
>
> 一期22项任务纳入D1—D10目标冲刺；二期11项仅保留待办，不纳入10天。配套：[plan.md](plan.md)、[框架设计](框架设计.md)、[项目评估](项目评估.md)。

## 1. 执行角色与周期口径

| 标识 | 执行者 | 责任 |
|---|---|---|
| G | gpt-6-astra | 只读代码调研、任务规划、架构/契约设计、独立代码审查与复验；不编写业务代码、测试代码或修复补丁 |
| L | glm-5.3-flesh | 所有模块代码、测试/fixture、脚本、迁移、缺陷修复与实现记录；按批准计划执行 |
| X | xueyangfei | 唯一项目负责人：范围、预算/权限、每天合并审查、真实视觉验收、变更及D10放行 |

这是任务分配，不是两模型能力排名。开发中已实际调用 director-planner、director-coder、director-reviewer；曾有审查请求被 provider 拒绝，重试后返回结果，但后台精确模型路由仍未验证。原规划中的 glm-5.3-flesh 与现有编码绑定 builtin:zai/GLM-5.3-Flash 不应当作已证明同一模型；本轮按已批准计划使用现有代理配置。本文不改代理配置，不以提示词切换模型；具体证据及未验证项见 implementation/DSK-002.md。

D1是X确认实际开工的日期；D1—D10为连续10个日历日工作窗口，不自动补周末或另加缓冲。启动日期未确定，不虚构绝对完工日。D9—D10主要用于回归/修复/验收，属于10天内部，不代表可以不做测试。

原人工55/22人日和周排期作废，不换算为“AI人日”或保证倍速。使用任务产物、关键路径和当天实测吞吐跟踪。二期收费/发行待一期结果后重新排期。

L是唯一代码写入者，同一工作树一次一个任务；G可提前规划下一任务或只读审查冻结差异，但不得同时修改代码或替L修复。X建议每天预留上午分派、午间解阻、晚间合并演示三个窗口（合计约2—3小时为排期假设），D1和D9—D10增加验收投入。若投入不足记录风险，不伪造评审完成。

## 2. 任务总表

代码编写统一为L，规划/审查统一为G，决策/最终验收统一为X。纯决策任务没有必须写代码的要求：G提供方案，X决定，L仅更新获准实现/记录。日期是目标执行窗，依赖未完成不得执行真实联调。

| ID | 任务 | 编码 | 规划/审查 | 依赖 | 目标日 | 阶段 | 状态 |
|---|---|---|---|---|---|---|---|
| DSK-001 | 源码迁入与桌面运行基线 | L | G | 无 | D1 | 一期 | 技术审查通过（人工待验收） |
| DSK-002 | 一期需求、UI方向与本地模型决策 | L | G | DSK-001 | D1 | 一期 | 技术审查通过（人工待验收） |
| DSK-003 | IPC与流水线共享契约 | L | G | DSK-001, DSK-002 | D1 | 一期 | 技术审查通过（人工待验收） |
| DSK-004 | 本地项目库与不可变快照 | L | G | DSK-003 | D2 | 一期 | SQLite门禁独立PASS；产品REWORK第1轮A段已实施待复审，B段待完成；已暂停，非产品验收通过 |
| DSK-005 | 新主题与任务式主界面 | L | G | DSK-003 | D2 | 一期 | 未开始 |
| DSK-006 | 五类参数化镜头套件 | L | G | DSK-005 | D2 | 一期 | 未开始 |
| DSK-007 | 状态机、审批与预算协调器 | L | G | DSK-004 | D2-D3 | 一期 | 未开始 |
| DSK-008 | 结构化拆镜与机位提案 | L | G | DSK-006, DSK-007, DSK-016 | D4 | 一期 | 未开始 |
| DSK-009 | 局部修改与镜头事务执行 | L | G | DSK-004, DSK-006, DSK-007 | D4 | 一期 | 未开始 |
| DSK-010 | 空间检查与有界修复接线 | L | G | DSK-008, DSK-009 | D5 | 一期 | 未开始 |
| DSK-011 | 暂停、取消与中断恢复 | L | G | DSK-007, DSK-009 | D5 | 一期 | 未开始 |
| DSK-012 | 3—5镜头编排与连续性 | L | G | DSK-010, DSK-011 | D6 | 一期 | 未开始 |
| DSK-013 | 桌面编码与确认写盘 | L | G | DSK-004, DSK-009 | D5 | 一期 | 未开始 |
| DSK-014 | 镜头事实与提示词同步 | L | G | DSK-008, DSK-009 | D5 | 一期 | 未开始 |
| DSK-015 | 逐镜头参考包与批次清单 | L | G | DSK-012, DSK-013, DSK-014 | D7 | 一期 | 未开始 |
| DSK-016 | 本地BYOK适配与调用记录 | L | G | DSK-003 | D3 | 一期 | 未开始 |
| DSK-017 | 设备登录与软件授权 | L | G | DSK-029, DSK-030 | 另行排期 | 二期 | 未开始 |
| DSK-018 | 托管AI账本与支付对账 | L | G | DSK-017, DSK-030 | 另行排期 | 二期 | 未开始 |
| DSK-019 | 账户、付费与权益界面 | L | G | DSK-017, DSK-018 | 另行排期 | 二期 | 未开始 |
| DSK-020 | 正式发行打包、签名与更新 | L | G | DSK-028, DSK-029 | 另行排期 | 二期 | 未开始 |
| DSK-021 | 单镜头与流水线AI评测 | L | G | DSK-010, DSK-011, DSK-012, DSK-016 | D7-D9 | 一期 | 未开始 |
| DSK-022 | 参考包与文件一致性验收 | L | G | DSK-015 | D8-D9 | 一期 | 未开始 |
| DSK-023 | 崩溃、睡眠与恢复回归 | L | G | DSK-011, DSK-015 | D8-D9 | 一期 | 未开始 |
| DSK-024 | 本地IPC、密钥与数据边界测试 | L | G | DSK-009, DSK-016 | D6 | 一期 | 未开始 |
| DSK-025 | 开发态硬件、缩放与性能验收 | L | G | DSK-005, DSK-013, DSK-023 | D9 | 一期 | 未开始 |
| DSK-026 | 官网、演示、许可与支持资料 | L | G | DSK-018, DSK-019, DSK-032 | 另行排期 | 二期 | 未开始 |
| DSK-027 | 受控部署、备份与预算告警 | L | G | DSK-018, DSK-031 | 另行排期 | 二期 | 未开始 |
| DSK-028 | 一期功能与UI验收交接 | L | G | DSK-021, DSK-022, DSK-023, DSK-024, DSK-025 | D10 | 一期 | 未开始 |
| DSK-029 | 二期收费、主体与发行决策 | L | G | DSK-028 | 另行排期 | 二期 | 未开始 |
| DSK-030 | 官方AI网关与持久服务任务 | L | G | DSK-029, DSK-016 | 另行排期 | 二期 | 未开始 |
| DSK-031 | 云权限、账务与数据故障测试 | L | G | DSK-018, DSK-019 | 另行排期 | 二期 | 未开始 |
| DSK-032 | 安装、签名更新与迁移验收 | L | G | DSK-020, DSK-025 | 另行排期 | 二期 | 未开始 |
| DSK-033 | 二期收费与公开发行门禁 | L | G | DSK-026, DSK-027, DSK-031, DSK-032 | 另行排期 | 二期 | 未开始 |

## 3. 10天逐日计划

| 日 | gpt-6-astra（G）：规划与审查 | glm-5.3-flesh（L）：串行编码与验证 | xueyangfei（X） |
|---|---|---|---|
| D1 | 规划001/002/003，审查源码迁入范围与契约 | 按批准顺序执行001→002记录→003契约与测试 | 确认模型真实绑定、环境、预算、范围和UI方向 |
| D2 | 审查快照/UI/模板设计，拆分最小补丁 | 串行004本地快照→005主界面→006五套件，每项交审 | 查看可操作UI/模板，关闭共享接口决策 |
| D3 | 规划并审查007状态机与016本地模型，检查调用预算 | 007→016编码/验证；处理D2审查返工 | G1：UI、模板、状态机与BYOK链路 |
| D4 | 规划008/009，独立核对scope与事务正确性 | 008提案→009局部事务；编写和执行相应回归 | 看真实创建、修改、撤销演示 |
| D5 | 按提交批次审查010/011/013/014，不代写修复 | 串行010检查→011恢复→013视频写盘→014事实文稿；未完成立即报告 | G2：单镜头闭环；任务密集，不假设四项当天必然完成 |
| D6 | 规划012连续性与024本地权限测试，审查结果 | 012多镜头→024本地边界测试；修复已确认缺陷 | 确认3—5镜头范围，不把未测能力标通过 |
| D7 | 审查015参考包，制定021评测规则与失败分类 | 015参考包→021评测代码及初轮执行 | G3：端到端参考包，冻结新增功能 |
| D8 | 独立审查021/022/023的差异与测试证据 | 串行021评测→022参考包验收→023故障恢复，优先补阻塞 | 查看文件和恢复录屏，明确P0/P1 |
| D9 | 对修复差异复审；必要时只读复验既有测试 | 完成021/022/023→025硬件/缩放/性能；只修复不扩范围 | 开始5人易用性观察，缺证据标未验收 |
| D10 | 输出最终PASS/REWORK/REPLAN审查意见，不替X放行 | 028交付材料、测试回归与批准的最后修复 | Go/Partial/No-Go；未过关不启动二期 |

所有实现、测试代码与返工都由L串行完成。G可在只读冻结基线上规划/审查，不构成第二条编码产能。同日每任务都要完成计划批准→编码→独立审查；依赖未通过不能跳过。

### 3.0 统一规划—编码—审查流程

1. 主会话按[AGENTS.md](AGENTS.md)准备源码基线、工作树初始状态与任务上下文。
2. G规划角色返回PLAN_READY或BLOCKED；X批准，已有明确批准且范围不变的计划不重复询问。
3. L编码角色按任务单实施，返回IMPLEMENTED和真实验证记录。
4. G独立审查角色检查实际差异、约束和验证证据，返回PASS、REWORK或REPLAN。
5. REWORK只交回L；同一任务连续两轮返工未通过，转G重新规划，范围变化重新交X批准。G不直接修代码来绕过审查。
6. PASS后仍由X完成实际运行/视觉验收和合并决定。主会话不绕过编码代理写业务实现。

实际调用director-planner/director-coder/director-reviewer前需核查当前会话是否暴露这些代理和正确model绑定；不可用即报告阻塞，不用普通代理冒充。相关配置在[工作流说明](.zcode/workflows/README.md)，本次不修改其文件或宣称模型路由已验证。

### 3.1 快速开发方法与范围防线

- 最大化复用原编辑器、AI host、工具校验、场景/视频/ZIP功能，不重写引擎、UI框架或模型编排平台。
- D1由G规划StorySpec/ShotPlan/Workflow事件、X批准、L实现；所有主进程/UI/共享文件都由L写入，G只审查并提出问题。
- 五模板保持既定边界，不新增角色商城、复杂图生场景、任意模型或收费代码。
- 优先贯通真实纵向链路；每次实现附测试，不能只用mock演示到D10。
- SQLite/框架替换若阻塞，在D2由X决定是否变更；不自行取消恢复或偷偷换存储语义。
- 10天是固定冲刺目标，不是已证实的产能。D5的L负责四个顺序任务，是最密集的风险点；只能复用已存在实现并当日验收，不能把未完成工作算作完成。D5单镜头不通或D7批次不通时，当日上报：X决定继续修复、缩减可展示范围或暂停；任何缩范围同步改四文档，不暗中降低完整验收标准。
- D10保留Partial结果选项，逐任务标明未完成和阻塞。Partial不等于一期全通过；没有批准不自动延期或启动二期。

### 3.2 门禁

| 门禁 | 日期 | 必须验证 |
|---|---|---|
| G0 | D1末 | 开发模型可用性/权限、源码启动、范围和契约 |
| G1 | D3末 | 新UI、五套件、本地模型接口与状态机 |
| G2 | D5末 | 真实单镜头、局部保护、取消恢复、视频与文稿 |
| G3 | D7末 | 同场景多镜头、完整参考包、停止新增功能 |
| G4 | D10 | AI/REF/恢复/本地安全/硬件/易用性证据与X签字 |

二期11任务保持另行排期，DSK-029仅在X确认一期验收结论后启动；不把二期内容塞进D9—D10。正式收费后14天商业观察也不属于这次10天。

## 4. 任务实施细则

### DSK-001：源码迁入与桌面运行基线

输入原源码与当前文档仓库，确认按带来源的基线快照导入或保留上游历史；保留LICENSE/NOTICE，禁止强推历史或直接覆盖原目录。核对package锁文件、资源、ignore和当前修改；使用独立开发userData并禁用上游自动更新检查，不申请签名或构建正式安装包。

验收：获授权后安装依赖、运行`npm test`、`npm run build`；读脚本前提再运行桌面启动/验证。记录Node/Electron版本、Windows/硬件、失败与已知问题；未运行明确标记，不能以静态检查替代。

### DSK-002：一期需求、UI方向与本地模型决策

由xueyangfei确认gpt-6-astra与glm-5.3-flesh的实际开发入口、权限、预算及设备；这两者为开发执行者，不自动成为产品内置模型。确定开发代号、Windows环境、五套件、简单/高级UI交互、一个批准的BYOK模型和测试预算；收费/商户/正式品牌和证书后置。

验收：有明确的功能边界、UI流程与示例任务；未配模型可用mock执行确定性功能，不因二期决策阻塞。

### DSK-003：IPC与流水线共享契约

定义StorySpec、ShotPlan、WorkflowState、审批、镜头快照、模型响应、错误码和事件；纯DTO不依赖DOM/Electron。IPC仅暴露获准操作并校验sender/参数。

验收：前后端共享fixture、未知字段/越界/版本不兼容拒绝；模型不能传任意执行代码或路径。由L编写mock供分阶段集成，G只审查其契约与覆盖。

### DSK-004：本地项目库与不可变快照

主进程选定并验证SQLite驱动、当前Electron运行ABI和事务（正式发行产物ABI在二期验收），建立本地日志、快照文件、版本迁移与项目锁。实现快照先写后登记及孤立项核对。

验收：写盘失败不前移指针，工程恢复有可信版本；不覆盖上游userData；可备份/还原，两个窗口不能同时写同项目。

2026-09-16 实施进展：`DSK-004-product` 单任务按批准计划完成编码（storage.v1契约增量、schema v1库、内容寻址快照、TTL/generation租约、备份恢复、renderer受管会话与最小项目库菜单），`npm test` 458全过，build/prepare/三套桌面回归退出0，真实Electron双进程租约竞争（busy/硬杀TTL接管generation+1/释放重获）与408KiB分块跨端hash一致已实测。状态IMPLEMENTED待独立reviewer审查；人工GUI闭环、断电、安装包未验收，见[实施报告](implementation/DSK-004.md)。

### DSK-005：新主题与任务式主界面

镜头列表+预演+简易属性+Agent进度；高级参数折叠，主题符合制作任务，状态有文字，不依赖颜色。

验收：两种窗口与缩放可操作，键盘焦点可见；新手明确下一步；不删除原高级功能。

### DSK-006：五类参数化镜头套件

同场景内人物、机位与动作模板参数化，stable roleId与entityId区分；内置资源许可明确。

验收：每模板无需AI即可实例化/播放；边界时长、画幅、人物数量校验；不把复制场景误当身份连续性保证。

### DSK-007：状态机、审批与预算协调器

实现框架阶段机、两次主要审批、阶段白名单、每镜头最多2次修复、计划/工作流预算上限。逐阶段记录状态。

验收：模型说“完成”不能跳过检查；超范围/超预算暂停；不启用上游无限maxRounds；暂停/取消不同。

### DSK-008：结构化拆镜与机位提案

受限剧情拆为3—5同场景镜头，或单镜头模式；模型提案编译为确定性操作，资产ID/角色映射校验。

验收：成功是可应用工程提案，不是建议文字；返回计划时长、连续/跳跃关系、范围与版本；用户缺失信息不编造。

### DSK-009：局部修改与镜头事务执行

在草稿空间预检、记录前后hash、持久快照后加载；camera-only/选中人物/当前镜头scope保护，批准工程保持不变。

验收：受保护字段变化为0；一次撤销恢复本任务前状态；资源准备期间发生revision冲突不覆盖；原内存预检不冒充持久事务。

### DSK-010：空间检查与有界修复接线

复用入镜/遮挡/区间扫描；错误分类为确定失败、警告、不确定；有限修复回到相同scope。

验收：记录采样范围/限制，缺资源/非法值必拒；射线或AABB检查不标为精确视觉证明；修复总次数受007限制。

### DSK-011：暂停、取消与中断恢复

协调模型AbortController、renderer job和编码取消；启动核对unknown意图、已持久快照与当前工程。

验收：提交前后/回执丢失可恢复不重复写；不能中断时显示PAUSING；停止模型不伪报已取消导出。

### DSK-012：3—5镜头编排与连续性

按确认计划逐镜头生成，roleId和场景基准稳定，镜头支持连续状态承接或明确时间跳跃；检查后整段审阅。

验收：一个镜头失败不重做已批准镜头；改上游镜头后相关下游依赖标待检查；不偷偷扩大到跨场景长剧。

### DSK-013：桌面编码与确认写盘

复用Mediabunny和渲染，24fps/720p，输出目录授权、临时文件与主进程写入确认；错误/取消恢复渲染状态。

验收：实际MP4或明确WebM回退可播放；磁盘满/权限/占用不返回成功；文件落盘后才报告完成，sleep后不承诺续帧。

### DSK-014：镜头事实与提示词同步

根据实际快照和范围提取事实；成片设定独立，规则中文编译；文稿哈希、过期与人工重绑。

验收：范围裁剪正确，不编造步数/外观；修改机位标过期，播放头不误报；规则生成不调用模型、不扣AI额度。

### DSK-015：逐镜头参考包与批次清单

同一冻结快照输出视频、首尾PNG、prompt、说明和manifest，总批次列成功/失败及哈希；逐项恢复。

验收：实际帧采样对应，完成文件不重复导出，失败批次不误报全成功，manifest无自引用hash；不默认打包完整工程或敏感路径。

### DSK-016：本地BYOK适配与调用记录

在主进程复用provider，提供连接测试、结构化输出、超时/取消、次数/token上限、usage记录和本地模型设置入口。只预留ProviderAdapter，不建官方网关或账户库。

验收：无账号/付款也可运行；密钥不入renderer/工程/日志，安全存储失败仅内存；未知响应不盲目重发。模型费用由供应商收取，不是软件收费。

### DSK-017：设备登录与软件授权

系统浏览器确认+device code轮询，安全保存会话；云签名软件权益、设备撤销和经DSK-029批准的离线缓存策略。

验收：令牌重放/过期/跨账号拒绝；缓存不超过许可期；过期保留查看备份普通编辑；不宣传不可破解。

### DSK-018：托管AI账本与支付对账

实现原子预留/结算/释放/补偿、每阶段请求预算与工作流服务端上限，官方响应持久后结算；支付验签去重、价格服务端决定。

验收：并发不透支，客户端报告失败不触发无限退款，断线重取不重扣；订单/退款乱序可对账；真实支付需单独授权验收。

### DSK-019：账户、付费与权益界面

显示软件许可、设备、AI余额、BYOK费用归属、工作流最大消耗、每次修复消耗；付款外开可信浏览器，由服务端查证结果。

验收：不以本地成功页发权益；用户明确计划生成本身可能计费；参考包重复导出无AI账单；无自动续费误导。

### DSK-020：正式发行打包、签名与更新

自有appId/userData/协议/安装信息，独立更新源，签名证书和验证；无任务时才更新，升级前快照备份。

验收：签名来源可验证，更新不走上游地址，TLS不关闭；不承诺签名必消除SmartScreen提示；此任务在二期完成发行链路，最终安装验证在DSK-032；一期DSK-025仅验证开发运行。

### DSK-021：单镜头与流水线AI评测

40有效创建/修改+10无效/恢复+10独立保留用例，另外10段3—5镜头工作流，固定模型/预算与失败定义。

验收：有效请求成功≥90%，保护字段零破坏，流水线至少8/10到人工审阅；输出时延、每有效镜头成本、修复与人工干预数据，不仅统计JSON合法率。

### DSK-022：参考包与文件一致性验收

执行框架20项REF，真实解压/播放与hash复算，首尾PNG/视频采用有损容差比较。

验收：所有规则测试通过；至少固定机位、跟拍、跨动作裁剪三包人工检查；未知模型未标支持。

### DSK-023：崩溃、睡眠与恢复回归

至少12个断点，覆盖快照写入、DB指针、renderer回执、导出中断、睡眠、关闭、文件占用/磁盘满。

验收：不重复应用、不覆盖新手动内容、不丢已批准镜头；恢复时重新核对产物，不依赖原内存receipt。

### DSK-024：本地IPC、密钥与数据边界测试

验证IPC sender与参数、文件路径权限、BYOK密钥、输入大小、日志脱敏和本地数据删除/恢复；云账号/资金测试留给二期。

验收：模型不能扩大权限，密钥无意外外传，默认不上传诊断；本地必要保护不因一期不收费而删除。

### DSK-025：开发态硬件、缩放与性能验收

从锁定依赖的Electron开发入口测试Windows集显/独显、窗口缩放和导出性能，不生成NSIS、签名或安装升级包。

验收：固定场景目标30fps，UI可用、输出可播放并记录硬件；当前Electron SQLite ABI可运行，发行产物ABI在DSK-032另验收。

### DSK-026：官网、演示、许可与支持资料

官网做下载/教程/价格/隐私/许可/支持，不做编辑器；保留MIT和第三方素材归属，录制真实创建/修改/恢复/交付。

验收：主卖点与功能一致，无“唯一/零联网/一键成片”；外部生成能力未验证不宣传；支持地区和服务政策由负责人审核。

### DSK-027：受控部署、备份与预算告警

单环境API/数据库部署、凭据注入、用量/错误/预算告警、备份与恢复演练；沿用已经测试的基础服务；二期重新估时，不继承旧人工日承诺。

验收：恢复后对账不重复grant；可停止新AI请求/支付但保留已交付结果；未获部署许可只交脚本和演练记录。

### DSK-028：一期功能与UI验收交接

组织5名目标创作者在准备好的开发环境测试创建、修改、多镜头、恢复与参考包。交付可重复构建启动说明、示例、录屏、测试证据和问题清单。

验收：至少4/5无需代操作完成核心任务，关键UI卡点关闭，AI/恢复/REF达标。一期P0为零；仅决定是否进入二期，不要求支付/证书或公开分发。

### DSK-029：二期收费、主体与发行决策

根据一期成本与反馈决定销售方式、价格/额度、续费、设备/离线策略、商户/地区、模型/邮件/支付和签名主体。

验收：负责人单独批准二期预算。原12个月软件/30天AI包/7天离线缓存只属候选，不是确定标准。

### DSK-030：官方AI网关与持久服务任务

在一期ProviderAdapter上增加Node/PG官方网关、有界持久任务、响应重取、限额和usage，不承接GPU渲染或全工程上传。

验收：官方密钥仅云端，同键不同输入拒绝，结果可重取；017/018未接入前只用沙箱，不开放匿名消费。

### DSK-031：云权限、账务与数据故障测试

验证跨账号/设备撤销、支付验签重放、并发余额、取消终态、补偿、服务重启及云内容清理。

验收：关键财务/授权全通过，客户端自报失败不能无限退款；不以一期本地测试替代。

### DSK-032：安装、签名更新与迁移验收

基于正式构建产物验证签名、非管理员安装、SQLite ABI/资源、升级/回退与项目兼容。

验收：不覆盖上游数据或更新源，未知schema不强制覆盖；证书未到位明确阻塞，不以开发启动代替发行。

### DSK-033：二期收费与公开发行门禁

汇总云支付/授权、模型回归、签名安装、主体条件、客服条款和预算告警，安排获准小额生产支付验收。

验收：xueyangfei的Go/No-Go批准后才收费/发安装包；其后14天商业观察单独执行。

## 5. AI 编码交接模板

以下是任务委派说明，不是shell命令：

```text
仓库：E:\myProgram\DirectorDesk\director-desk-web
负责人：xueyangfei；规划与独立审查：gpt-6-astra；唯一编码者：glm-5.3-flesh。
路由按AGENTS.md，先核查实际代理和model绑定；不可用报告BLOCKED，不冒充。
先读四份文档、适用AGENTS和源码，检查当前D日、依赖与文件锁；一期禁止执行二期任务。
仅执行一个已批准DSK任务。源码未迁入先停在DSK-001，不在原目录误改。
先明确改动文件、契约和测试，再小步实现，不改技术栈或扩大功能范围。
默认用mock/沙箱，不使用真实密钥、模型费用、支付、部署或推送权限。
结束报告：实现文件、迁移影响、执行测试及真实输出、未执行项、遗留问题、下一任务。
遇到权限、账务、恢复或范围歧义，记录具体阻塞，不自行降低验收。
```

每个任务交接必须写出实际开发模型标识、base commit、文件归属、接口版本与测试结果。G负责独立审查，L负责实现和修复，二者均不能自己批准合并；由xueyangfei确认。开发模型与产品运行时BYOK模型分开记录。

每任务记录可在实施时创建 `implementation/DSK-编号.md`，包含开始/结束源码版本、测试环境、契约变化、操作复现、真实费用授权、失败项和人审结果；不预生成空文档。

## 6. 重排与范围纪律

关键链延迟超过半天或当天门禁失败、模型/支付更换、SQLite/打包驱动改变、输入场景扩大或加入macOS时重排。四文档一起修改，不只改日期。D9—D10是冲刺内修复/验收时间，二期另排，不自动追加日程；任何云工程、多Agent、最终视频API、专用模型适配均另立任务估算。
