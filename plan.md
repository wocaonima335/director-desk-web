# AI 短剧桌面预演工作台：产品与实施计划

> 版本：DESKTOP-04。本文件及同目录三份中文文档是当前唯一有效规划，替代原网页路线与WEB任务编号。
>
> 状态：待实施；文档中的新增能力尚未完成。依据上游DirectorDesk 0.4.8、Git `eee9234`的静态检查，未运行验收或真实付费评测。
>
> 当前仓库名称仍为 `director-desk-web`，只是历史命名，不表示继续建设网页编辑器；本次不改名、不迁源码、不部署、不提交推送。

## 1. 文档与基线

- [任务列表](任务列表.md)：一期22项的D1—D10逐日分工；二期11项待办另排，不再使用人工人日估算。
- [框架设计](框架设计.md)：Electron边界、Agent状态机、恢复、模型服务、授权计费与参考包契约。
- [项目评估](项目评估.md)：证据、差异化、投入、风险与商业验证。
- [上游项目](https://github.com/mangfufu/director-desk)、[上游许可](https://github.com/mangfufu/director-desk/blob/eee9234/LICENSE)。

原源码在 `E:\myProgram\DirectorDesk\director-desk`；商业规划仓库在 `E:\myProgram\DirectorDesk\director-desk-web`。开始代码工作前由DSK-001确认迁入方式、保留许可和来源，不直接改动上游工作区或强推历史。

### 1.1 项目组织与10天目标

- 项目负责人：**xueyangfei**，负责范围、环境/费用授权、集成审核、真实画面与功能验收、Go/Partial/No-Go。
- 规划与代码审查：**gpt-6-astra**（G），负责任务拆分、架构/契约方案、独立审查，不直接编写或修改业务/测试代码。
- 代码编写：**glm-5.3-flesh**（L），负责全部模块、测试、脚本、迁移和缺陷修复；同一工作树一次一个编码任务。
- 一期周期：**D1—D10连续10个日历日**，由xueyangfei确认实际开工日；没有确认日期前只使用相对D日，不自定截止日。
- D1核对两个模型的实际可调用标识、开发工具权限、额度与上下文；本次仅修改文档，没有启动它们。开发模型不自动等于产品运行时的BYOK模型。
- 每天三个短审查窗口，建议负责人合计预留2—3小时，D1和D9—D10增加测试投入；实际可用性不满足时记录风险。

### 1.2 调度与模型绑定

遵守[AGENTS.md](AGENTS.md)：planner规划→xueyangfei批准→coder编码→独立reviewer审查；REWORK交回GLM，连续两轮未过转GPT重规划；最终运行验收与合并由负责人决定。具体任务单与路由见[工作流说明](.zcode/workflows/README.md)。

文档中的编码名称按最新要求使用glm-5.3-flesh，但现有说明列出的实际绑定是builtin:zai/GLM-5.3-Flash。二者不自动视为同一模型；本次不改配置，实际切换需核对可用model ID并获批准。代理不可用不冒充调用成功。

## 2. 产品定位

一句话：把一段短剧情节，变成可编辑、可检查、可交付的镜头参考。

首批客户：已有AI短剧或剧情短片项目、需要机位与人物调度、但不熟悉复杂三维软件的创作者。不是面向所有视频用户，不以大型影视虚拟制片为首发目标。

付费价值：少学复杂参数、稳定局部修改、按计划自动生成草稿、失败可恢复、视频与提示词不混版。基础编辑器来自开源项目，应透明说明；主题换色或聊天窗口本身不是充分付费理由。

产品不负责最终风格化影片生成，不保证任何下游模型逐帧遵循预演，也不宣传完全无人值守或自动替代导演审美。

## 3. 首发范围

### 3.1 平台与任务限制

- Windows 11 x64优先，Electron桌面应用；macOS签名、公证及适配另立版本。
- 复用TypeScript、Vite、原生DOM、Three.js及现有Electron，不先迁移UI框架或渲染引擎。
- 五类任务：双人对话、人物入场/离场、跟拍、推进/环绕、简单对峙。
- 单镜头5—15秒，一个连续机位，最多两名主要人物。
- 流水线限定同一场景的3—5镜头，总时长15—75秒；每镜头独立草稿和检查点，共用角色身份与场景基准。
- 720p、24fps参考视频为基础验收规格；支持9:16、16:9。1080p仅在实际设备验证后列为支持，不预承诺4K。
- 一个项目同时一个写入/渲染工作流；不做并行多窗口GPU导出。

### 3.2 一期与二期交付

**一期：项目构建、功能实现与UI优化。** 源码迁入、锁定依赖、Electron开发启动；新主题/简单模式、五套件、单镜头创建修改、3—5镜头流水线、检查恢复、参考视频与对应提示词。采用主进程BYOK/mock，无软件登录、激活、计费、官方云服务或正式安装包依赖。

一期交付源码、构建/开发启动说明、示例工程、功能/UI验收记录与演示录屏。测试可在预先准备的开发环境进行，不为测试而提前建设发行体系。

**二期：商业化与发行。** 一期验收后单独决定收费标准、许可和续费策略；实现官方AI网关、设备登录授权、支付账本、账户界面、正式安装包、签名、公证（若后来批准macOS）、更新回退、官网和公开发行门禁。

区分“构建”和“发行”：一期必须能编译资源、运行桌面程序、验证当前Electron/SQLite ABI；二期才制作发布用安装产物和自动更新。开发态数据目录隔离、保留许可、禁用上游自动更新检查在一期完成，不需要先申请证书。

### 3.3 两期之外的后续能力

完整网页编辑器、移动编辑、云工程同步、实时协作、公共远程MCP、任意用户技能自动执行、任意模型市场、图片自动重建复杂场景、跨多个地点长剧情、最终视频生成API、云渲染、自动多语言配音、专业审片协作。

首发参考包使用中文规则提示词；AI文案润色/翻译、MiniMax/Seedance专用适配后置。SD 2.0含义未确认，不猜测等于Seedance，Stable Diffusion 2.0不能直接视作原生视频参考模型。

## 4. 用户主流程

1. 启动桌面程序，打开示例或创建本地项目，选择存储目录。
2. 选择“设计一个镜头”或“短段落自动预演”。
3. 输入剧情、人物、场景与风格；系统标出缺失信息，不擅自编造角色外观。
4. AI生成结构化镜头计划，展示时长、调度、机位、模型调用上限与估算成本（非软件收费）。
5. 用户确认计划，授权在该计划对应的草稿空间自动搭建与修复。
6. 协调器逐镜头运行，显示阶段、检查结果和可恢复错误。
7. 用户整体预览、确认或局部修改；未通过镜头不得伪装成已完成。
8. 选择输出目录、镜头与覆盖规则，确认后批量生成视频、首尾帧、提示词及清单。
9. 用户自行把适合的素材交给外部模型，软件不自动上传工程或登录外部账号。

主要确认点只有“镜头计划”和“最终输出”；中间合规操作在已批准范围内自动执行。超过预算、变更角色/镜头数量、修复失败、路径冲突或覆盖现有文件时暂停确认。

## 5. UI 与视觉主题

主界面：左侧镜头卡片，中间大预演画面，右侧当前镜头简单参数，底部任务进度和自然语言修改入口。

- 深色中性背景、清晰对比和一组品牌强调色；颜色配合文字/图标表达状态，不只靠颜色。
- 镜头状态：待设计、生成中、检查失败、待确认、已批准、导出中、已交付、已过期。
- 默认显示景别、角度、运镜、时长、人物动作以及“保持不变”的对象。
- 坐标、曲线、完整时间轴和复杂效果进入高级模式；保留现有操作能力，不把隐藏按钮当作功能删除。
- 手动调时长、画幅、位置无需调用AI或扣AI额度。
- 明确“暂停后可恢复”“取消当前步骤”“撤销本镜头修改”的区别；不在同一按钮上混用。
- 任务执行中锁定受影响工程写操作；允许在安全检查点暂停后修改。渲染采样期间禁用并发预览拖动。
- 首发验证1280×720与1920×1080窗口及125%/150%缩放，键盘焦点和错误信息可读。

## 6. Agent 自动化原则

一个确定性协调器管理一个受控Agent。逻辑上可分“拆镜、调度、摄影、检查”，但首发不引入多个独立Agent互相聊天。

流程：输入结构化 → 镜头计划 → 计划确认 → 镜头草稿 → 调度/机位编译 → 程序检查 → 有限修复 → 人工预览批准 → 导出批准 → 写盘校验 → 完成。

模型只输出结构化提案；不能自由执行脚本、支付、任意本机路径、网络抓取和项目删除。每个阶段有工具名和参数白名单，校验同时发生在主进程和渲染端。

初始预算：计划阶段最多2次模型请求；每镜头1次初稿+最多2次修复，即最多3次；5镜头首次流程最多17次。次数只是硬上限，还需token、金额和超时上限，以最先达到者为准。不沿用无限maxRounds选项。用户新增修改视作新请求，先明确预算。

程序检查可以发现非法参数、受保护字段变化、部分出画/遮挡和空间异常；采样检查不等于连续碰撞证明，更不等于叙事或表演质量。检查不完整应显示“不确定”，而非通过。

## 7. 参考视频与提示词交付

每个已批准镜头输出一个目录，批次另有总清单：

```text
project-delivery/
  batch-manifest.json
  README.md
  shot-001/
    preview.mp4
    first-frame.png
    last-frame.png
    prompt.txt
    reference-guide.md
    manifest.json
  shot-002/
    ...
```

WebM回退时文件名、MIME和说明同步变化。每个镜头是同一冻结快照，提示词时间相对于实际导出片段；首尾帧来自相同渲染采样序列。末帧不是结束时间点，不能混入下一镜头。

规则提示词从实际工程提取事实，合并用户成片设定；未知动作、步数、服装、情绪不虚构。用户改镜头或风格后标记文稿和导出过期；人工修订需确认，不被自动覆盖。

参考包默认不含完整工程、密钥、账号、本机路径或对话；工程可单独备份。通用包不宣称某模型支持视频输入。批次只有全部选中镜头写盘并校验成功才标完成；部分失败可从失败镜头继续，不能盲目重导已成功项。

详细帧采样、清单和恢复规则见[框架设计](框架设计.md)。

## 8. 一期不收费，二期再定义商业规则

一期不制定软件价格、额度套餐、续费期限或离线授权天数；不做登录墙、激活、会员限制、订单、付款或官方云数据库。已有功能通过本地开发入口可直接使用。

BYOK只表示使用获准模型密钥，供应商仍可能收费。保留连接测试、调用/token/时间上限、明确的剩余调用提示与本地usage记录；缺少可靠价格表时只显示用量，金额标为未知，不虚报零成本。模型未知响应不盲目重试。

规则提示词、手动修改、导出不调用模型。一期不上传官方服务器，无账号不等于无文件/IPC权限控制。

二期DSK-029根据一期质量、成本和反馈决定软件许可、托管AI包、自动续费与否、设备和离线策略。之前提过的12个月许可、30天AI包、7天离线均降为待评估候选，不是当前承诺。二期官方共享密钥必须留云端，账务/付款规则通过专门门禁后才能开放。

## 9. 分期验收门槛

所有数字是目标，不是当前结果。

| 门槛 | 目标与证据 |
|---|---|
| 单镜头质量 | 40条有效创建/修改用例，成功率≥90%；10条无效/恢复用例正确拒绝/恢复；另10条保留有效用例不用于调提示词 |
| 范围安全 | camera-only等受保护字段变化为0；失败不污染已批准工程 |
| 多镜头流程 | 10段3—5镜头场景，至少8段无需开发者修代码即可到待人工审阅；所有失败可定位到步骤 |
| 一期恢复 | 至少12个断点/睡眠/关闭/磁盘故障场景，不重复写入/模型重发或误报完成 |
| 参考包 | 框架列出的20项REF测试全通过，文件可解压/播放、哈希可复算 |
| 二期资金与授权 | 并发余额、支付回调、补偿、跨账号访问等关键用例全通过 |
| 二期Windows发行 | 安装/更新/回退、数据迁移、签名验证在约定设备矩阵通过 |
| 易用性 | 5名目标创作者至少4名无需代操作完成单镜头、局部修改与参考包；目标中位10分钟内 |

一期另在开发启动环境检查硬件/缩放/性能，不要求签名安装包。D10的G4只判断功能和UI是否可交接；二期财务和发行门槛不能反向阻塞一期。

二期收费放行后另观察14天：建议10名目标创作者中至少3名真实付费，其中至少2名用于第二项目。小样本仅作继续决策，不当作行业转化率。

下游效果必须另做直接生成与参考包生成对照；若目标入口不支持视频参考，只测试其支持的首帧/文本方案，不偷换比较口径。

## 10. 一期10天快速开发计划

一期22项任务保留既定功能目标，改以产物与每日门禁追踪，不把原55人工人日压缩成10个“AI人日”。执行方式为gpt-6-astra规划/审查、glm-5.3-flesh唯一编码、xueyangfei最终决策；不再假设两模型并行写代码。规划审查与编码可以交接，但同一任务按批准→实施→审查顺序推进。

- D1：源码启动、开发模型与预算核验、UI/IPC契约冻结。
- D2—D3：新主题UI、五套件、本地快照、状态机、BYOK。
- D4—D5：真实单镜头创建/修改、范围保护、检查、取消恢复、视频与文稿。
- D6—D7：同场景3—5镜头、参考包与批次清单，D7冻结新功能。
- D8—D9：AI评测、20项REF、故障恢复、本地安全、硬件和易用性。
- D10：修复受影响问题、回归、交付源码/启动说明/示例/录屏，由xueyangfei判断Go/Partial/No-Go。

改为单编码者后，D2/D5及审查返工更集中，旧双写产能假设撤销。10天仍为快速冲刺目标，不是没有实测依据的完工保证；D9—D10已包含修复验收，不另自动添加缓冲。若D5/D7关键链未通，当天由负责人决定优先修复或显式调整范围。任何缩减需同步文档与交付标记，不能用mock或删测试假报完成。5名测试参与者由负责人在D1开始预约；未齐备时保留未验收项。

二期11项只保留收费、账号、官方托管与发行待办，排期/投入在一期结果后重新估算。本次10天不包含二期或14天付费观察，不预先固定二期4周。

## 11. 宣传与增长

标题：把短剧情节，变成可以预演的镜头。

副标题：AI辅助搭景、人物走位和运镜设计，可局部修改、自动检查，并输出参考视频与对应提示词。

一期只制作内部验证素材，不建设公开收费/下载官网；二期才做官网与推广。

优先素材：完整剧情到预演演示、只改机位的对照、中途失败恢复、可下载工程。官网承担演示、下载、价格、许可和教程，不继续做在线编辑器。

优先渠道：真实工作室试点、B站/YouTube可复现教程、制作社群。SEO围绕“AI短剧人物走位”“运镜预演”“参考视频提示词”等任务型词，但尚未验证其搜索量。

未经实测不宣传一键完整短剧、百分百控制、全平台支持、零联网、全市场唯一或固定降本比例。改进指标以有效镜头时间、复用、支持成本和付费为主，不以聊天字数或下载数代替价值。

## 12. 开工与变更规则

由xueyangfei确认实际启动后从DSK-001开始，不因本文存在而自动实施代码。DSK-002只确认一期功能/UI/开发代号/BYOK及测试预算；正式品牌、价格、销售主体、支付/邮件、许可与证书在二期DSK-029单独批准，不得阻塞一期。

新需求必须同时修改四份文档的范围、依赖、每日交付和门禁。源码尚未迁入，不要在历史上游目录误开发。旧文档从当前工作树移除，已提交历史仍可追溯；不改写Git历史。
