# AI 镜头设计商业网页版实施计划

> 状态：待批准的开发方案，不代表功能已经实现。本文仅新增规划文档，不授权自动实施、部署、购买服务或处理真实付款。
>
> 产品工作代号：ShotPilot Web（内部代号，未核查商标或域名，不作为正式品牌）。
>
> 基线：DirectorDesk 0.4.8，Git `eee9234`。规划日期：2026-09-13。基于静态代码检查，尚未执行构建、测试、性能或浏览器验收。
>
> 首发策略：浏览器端三维预演 + 简化操作 + AI 创建单镜头与局部修改 + 有限额度付费。不是最终视频生成器，不承诺下游模型百分之百遵循参考视频。

## 1. 文档导航与执行边界

- [AI 开发任务清单](docs/ai-development-tasks.md)：任务编号、依赖、改动边界、验收和交接格式。
- [开发排期](docs/development-schedule.md)：人力假设、周计划、工作量、关键路径、阶段门禁。
- [视频与提示词参考包规格](docs/video-reference-package.md)：REF-01范围变更、时间采样、提示词同步和验收。
- [上游说明](https://github.com/mangfufu/director-desk/blob/eee9234/README.md)、[MIT 许可](https://github.com/mangfufu/director-desk/blob/eee9234/LICENSE)。新仓库尚未迁入源码，这两项链接指向上游基线。

实施前先确认本计划，创建业务开发分支，再执行任务。不得因读取本文件而自动开始开发。文中的新增目录、API、数据表、脚本均为设计目标，不是现有能力；任务完成后必须以实际代码更新文档。

核心原则：先实现可靠的一个镜头，不先建设完整影视平台；换皮必须减少步骤，AI 必须改变可编辑工程而非只回复建议；不重复实现上游已经拥有的编辑与导出能力。

## 2. 产品定位与用户任务

### 2.1 产品定义

一句话定位：描述你想拍的镜头，AI 帮你安排机位和人物走位，你可以预览、修改并导出参考视频。

首批人群：正在制作 AI 短剧、剧情短片的个人创作者与小团队成员；需要复杂镜头，但不熟悉完整三维软件。首发中文、桌面浏览器优先。

不优先服务：大型虚拟制片、重型资产管线、移动端专业编辑、实时多人协作、只想一键得到风格化成片的泛娱乐体验者。

### 2.2 三个主要用户故事

1. 创建：选择走廊和两个人物，输入“女生在前，男生后退两步，镜头从女生身后推进”，得到 5—15 秒单镜头预演。
2. 修改：选择摄影机，输入“保留人物位置与走位，只改为侧面跟拍”，人物及其他对象不应被重做。
3. 交付：播放确认，填写角色外观与成片风格，导出同一片段的视频、首尾帧、对应提示词和素材清单；工程单独备份。最终生成在外部平台完成，不包含其费用。

首次任务默认一个场景、一个摄影机、最多两个人物；五类模板为双人对话、人物入场/离场、跟拍、推进/环绕、简单对峙。这里的范围约束属于 AI 引导模式，不删除高级编辑器既有功能。

### 2.3 价值与验证

| 假设 | 验证方式 | 失败后的处理 |
|---|---|---|
| 简化界面降低门槛 | 5 名目标创作者完成固定任务，与原界面对比 | 优先修任务入口，不继续堆功能 |
| AI 能完成镜头而不是只聊天 | 固定评测集检查工程与实际画面 | 缩小模板和指令范围 |
| 局部修改比重新生成有价值 | 检查保护字段和再次修改的使用情况 | 加强范围选择与确定性变换 |
| 存在付费需求 | 小范围真实付费试点和第二项目复用 | 不扩大投流，复访需求与报价 |

不能把下载量、注册量、对话字数或一次精彩演示当成商业验证。

## 3. 当前代码基线

| 能力 | 现有位置 | 本项目处理方式 |
|---|---|---|
| 浏览器入口与构建 | `index.html`、`package.json` 的 dev/build/preview | 复用 Vite，不先重写框架 |
| Three.js/WebGL 预演 | `src/engine.ts`、`src/main.ts` | 保留渲染和编辑核心 |
| 场景与多戏段 | `src/scenes/`、`src/model.ts` | 首发 AI 限定当前戏段 |
| AI 面板 | `src/ui/ai-panel.ts` | 当前依赖 `window.directorDesktop`，抽象传输层 |
| 桌面 Agent/provider | `desktop/ai-host.cjs`、`desktop/providers.cjs` | 参考逻辑，不将 fs/safeStorage/密钥打包进网页 |
| 自动化执行器 | `src/automation/service.ts` | 复用校验、revision、requestId、预检和提交 |
| AI 修改记录 | `src/automation/edit-journal.ts` | 用于摘要；不等于持久版本库 |
| 本地保存/恢复 | `src/storage.ts`、`src/ui/project-save.ts` | 新品牌存储隔离、迁移和备份提示 |
| 视频编码与交付 | `src/export.ts`、`src/exporting/delivery.ts` | 能力探测，支持失败回退 |
| 已有测试 | `tests/`、`npm test`、`npm run smoke` | 先记录基线；smoke 的前提须检查脚本 |

重要边界：`director_apply` 的 `preview:true` 是编辑预检，不是独立画面的候选预览；已有 receipts/previews 为内存映射，不是持久任务数据库，也不能用于计费。

## 4. 首发范围与不做事项

### 4.1 首发必须完成

- 自有品牌和首屏，去掉网页不支持的桌面渠道配置/MCP入口。
- 简单/高级模式切换，AI 引导默认简单模式。
- 五个成熟单镜头套件及许可清晰的内置角色与场景。
- AI 创建镜头、限定范围修改、执行摘要、取消、撤销与失败保留工程。
- 服务端模型访问、身份、服务端额度、支付确认、用量账本。
- 本地自动恢复、工程下载、参考视频导出、浏览器兼容提示。
- 通用单镜头参考包：同快照视频、首尾帧、规则提示词、使用说明与manifest；修改过期提醒，确定性生成和下载不扣AI额度。
- 有限任务记录、隐私说明、账号删除申请/处理与客服入口。
- 示例工程、完整操作演示、准确的产品/价格/许可页面。

### 4.2 明确后置

AI文案润色/翻译、MiniMax/Seedance专用适配、自动上传、多镜头参考包、云工程同步、实时协作、版本分支、审片审批、全项目生产交付、多镜头自动编排、公共 MCP、用户自定义远程技能、任意资源 URL 抓取、模型市场、最终 AI 视频生成 API、云渲染、移动完整编辑。

首发账户只同步权益与有限任务元数据，不代表工程已云备份。工程默认在当前浏览器；清理浏览器数据可能导致丢失。付费前和保存入口均须明确说明。

## 5. 交互与信息架构

### 5.1 首次体验

1. 首屏查看真实演示，无需登录可打开本地示例。
2. 进入编辑器，选择“描述镜头”或“套件起步”。
3. 请求云 AI 前登录、展示将发送的上下文类型及任务额度。
4. 输入需求，选择场景、人物和 5/10/15 秒时长。
5. 展示任务进度和结果摘要，默认应用通过校验的结果并立即可撤销；破坏性操作不在首发允许范围。
6. 播放预演，快捷调整或输入局部修改。
7. 下载工程备份，选择参考包范围与成片设定，确认提示词后导出视频与对应资料。

主布局：左侧 AI 与任务记录，中间预演，右侧简易景别/角度/运镜/时长控件，底部播放条；高级时间轴、坐标、复杂效果折叠。不通过模态窗口阻塞普通手动调整。

### 5.2 简化而非隐藏一切

手动改时长、画幅、对象位置和基础镜头无需消耗 AI；不能强制每次操作走模型。创建任务期间锁定当前工程的写操作与导出，允许播放查看、取消和下载已保存备份；其他标签页的变更不能被悄悄覆盖。

状态必须区分：排队、规划、生成提案、校验、待提交、完成、失败、已取消、结果待恢复。禁止长期只显示“思考中”。

## 6. 推荐技术架构

### 6.1 技术选型与目录

保留现有 TypeScript + Vite + 原生 DOM + Three.js。新增独立 Node.js 24 LTS/TypeScript API 服务，推荐 Fastify + Zod；数据库 PostgreSQL。服务端版本需锁定并验证，不在编写文档阶段安装依赖。

首发使用 PostgreSQL 持久任务表与有界 worker 租约队列，避免进程重启丢任务；不额外引入 Redis/BullMQ，达到需要时再替换队列适配器。长请求不要放在短超时 serverless 函数里。反向代理以同站点 `/api` 暴露服务，前端静态资源可走 CDN。

建议新增结构（尚未创建）：

```text
server/src/
  auth/             登录会话与用户
  ai/               provider、任务、限额、worker
  billing/          权益、额度账本、支付适配器
  http/             路由与错误映射
  db/               schema、迁移、租约与事务
  observability/    脱敏日志、指标、成本
src/web/
  ai/               网页传输、提案校验、任务恢复
  onboarding/       简单模式与首次体验
  templates/        参数化镜头套件
  account/          权益展示
  reference-package/ 范围快照、成片设定、文稿版本与打包协调
shared/web-contracts/
  schemas.ts        可序列化 DTO，不依赖 DOM/Electron
```

共享依赖保持单向：浏览器与 API 依赖纯 DTO，DTO 不依赖引擎。`src/main.ts` 只做接线，不发展成新的业务聚合文件。保留 desktop 实现作为另一种 transport，网页隐藏不支持功能。

### 6.2 浏览器与服务端职责

| 浏览器 | 服务端 |
|---|---|
| 工程、3D 预演、渲染、导出 | 登录、权限、用量账本、支付 |
| 本地获取当前选区与规范化快照 | 有界调用模型，输出结构化提案 |
| 使用现有执行器预检与提交 | 再次校验 schema/修改范围/资源清单 |
| 本地 revision 冲突检查 | 持久任务状态、幂等、队列、成本监控 |
| 局部备份与恢复 | 有限期保存提案供断线恢复 |

服务端不具备可信的浏览器渲染状态，不能把客户端的“已应用”当成唯一计费依据；也不能承诺云端检查所有画面质量。

### 6.3 首发 AI 采用有界提案，而非远程任意工具循环

优先流程：浏览器快照 → 服务端语义规划 → 模板/编辑编译 → 服务端验证 → 持久提案 → 浏览器预检与提交。只允许内置资源和允许操作，不给模型终端、浏览器导航、支付、文件系统、跨项目删除等能力。

先用固定结构描述人物/机位/轨迹，再编译成现有 `EditOperation[]`，避免让模型直接写整份无约束工程。允许复杂操作的位置必须经 schema 及业务范围双重限制。

首发每任务最多 1 次规划 + 1 次生成/修复 + 1 次有限修复（总模型调用上限 3）；一个账号同时 1 个活跃任务；操作数默认最多 40；总模型超时目标上限 120 秒。以上为初始可配置上限，在真实模型基准后调整，不沿用桌面的 64 轮或无限设置。

## 7. AI 契约与执行一致性

### 7.1 输入与输出

输入 DTO 包含：客户端任务 UUID、任务类型 create/modify、baseRevision、snapshotHash、schemaVersion、templateVersion、选区与允许字段、镜头时长、提示文本、规范化当前场景快照。完整二进制素材、API 密钥、全量无关历史不上传；首版可限制 JSON 上限 256 KiB，超限返回可操作错误。

服务端对输入重新校验；客户端不能通过传 model/baseUrl/credits/price 获得任意模型调用或自定价格。

输出包含：proposalId、服务端 taskId、baseRevision、输入快照摘要哈希、schemaVersion、templateVersion、operations、允许变更摘要、警告和计费单位。实体 ID 必须确定性生成或建立映射；修改已有对象不得随意替换 ID。

### 7.2 范围保护

默认只修改当前镜头。支持 camera-only、selected-entities、full-current-scene 三个受控范围；首版创建通常在新场景完成，避免破坏已有工作。

“只改摄影机”的校验应比较应用前后规范化工程，保护人物位置、走位、动作、场景物体和非目标机位。不能只检查自然语言提示词。锁定对象不允许变更。超时和无效提案不得部分写入工程。

### 7.3 预检、提交、撤销与恢复

1. 保存本地任务前快照，生成本地 documentId 与 revision。
2. 收到提案时再次比对 revision 与快照哈希；当前工程已变则不自动应用。
3. 执行 `director_apply` 预检；previewId 只在当前 renderer 生命周期有效，不持久化复用。
4. 使用全新 requestId 提交同一预检提案，成功后记录本地 appliedProposalId；写入结果快照并释放任务锁。
5. 一个首版任务仅一个提交批次，确保一次撤销恢复任务前状态。若运行验证发现上游历史不能保证此行为，必须补事务式快照后才能上线。
6. 刷新后不能盲目重放；比较本地 proposalId、前后快照哈希以及服务端持久提案。旧 renderer 的 revision 不可直接复用。
7. 不一致状态交给“保留当前工程/恢复任务前工程/在副本应用”，默认保留当前工程。

UI 的视觉质量与模型的语义正确性仍需播放验证；预检成功只能证明约束通过，不代表艺术质量。

## 8. API 与数据模型草案

### 8.1 API 草案

| 方法与路径 | 用途 | 关键约束 |
|---|---|---|
| `POST /api/auth/email/start` | 发送一次性登录链接 | 频率限制、不泄露邮箱是否注册 |
| `POST /api/auth/email/verify` | 用户确认后消费令牌建立会话 | 一次性、过期、避免邮件预取自动登录 |
| `POST /api/auth/logout` | 退出会话 | 撤销服务端 session |
| `GET /api/me` | 当前身份 | 不返回敏感凭据 |
| `GET /api/entitlements` | 权益与余额 | 服务端计算，不信客户端缓存 |
| `POST /api/ai/tasks` | 创建任务并预留额度 | 用户+幂等键唯一、同键不同输入拒绝 |
| `GET /api/ai/tasks/:id` | 查询进度/结果 | 所有权验证；短轮询先行 |
| `POST /api/ai/tasks/:id/cancel` | 尽力取消 | 根据持久状态确定释放/结算 |
| `POST /api/ai/tasks/:id/feedback` | 应用结果及诊断 | 仅反馈，不直接触发无条件退款 |
| `POST /api/billing/checkout` | 创建订单 | 服务端产品目录和金额 |
| `POST /api/billing/webhooks/:provider` | 接收付款通知 | 原始请求体验签、去重、事务入账 |
| `GET /api/billing/orders/:id` | 查订单 | 不能用前端支付成功参数发额度 |
| `POST /api/account/deletion-request` | 请求删除 | 再认证、停用任务、法定记录例外 |

状态错误包括 401、403、409、413、422、429、503；业务码至少包括 INSUFFICIENT_CREDITS、REVISION_CONFLICT、SCOPE_VIOLATION、PROVIDER_TIMEOUT、EXPORT_UNSUPPORTED、TASK_RECOVERY_REQUIRED。返回 requestId，不暴露密钥或 provider 原始堆栈。

### 8.2 持久实体

| 表/实体 | 关键字段与约束 |
|---|---|
| users / sessions / login_tokens | userId、验证时间、令牌摘要、过期、撤销 |
| ai_tasks | ownerId、幂等键、输入哈希、状态、lease、attempt、预算、usage、过期时间 |
| ai_proposals | taskId 唯一、版本、规范化操作、摘要哈希、保留期限 |
| credit_grants | 来源、金额单位、有效期；试用和购买额度分开 |
| credit_reservations | taskId 唯一、额度分配、状态 |
| credit_ledger | 不可改写分录；reserve/settle/release/reversal、唯一业务事件 |
| orders / payment_events | provider 订单ID与事件ID唯一、币种、金额、状态 |
| deletion_requests | 发起、处理中、完成、保留例外 |

额度使用整数，不以浮点货币计算。到期额度被任务预留后如何处理必须确定：预留有效时允许该任务结束并结算，释放时恢复原到期时间，不重新延长额度。API 不公开数据库递增标识作为授权依据。

## 9. 收费与成本控制

### 9.1 首发商品决策

界面采用“创作者月度额度包”，首发为一次性购买 30 天权益，不自动续费；后续再做循环订阅。手动编辑不计 AI 额度。通用参考包的规则文稿、手动修订、视频/帧打包与重复下载不计AI额度，首发不调用模型润色。试用只给有限额度；模板与权益配置在服务端。

建议测试 ¥49—99/30 天的报价，属于研究假设，不是已批准上线价格。必须先通过任务成本基准，确定每包额度、最大成本与毛利。最终视频模型费用不包含。

### 9.2 额度状态机

`queued/reserved → running → succeeded/settled`；失败或交付前取消进入 `failed|canceled/released`。running 的租约过期由恢复 worker 处理，不能无限占用余额。

结算定义：服务端已生成通过自身 schema/业务范围校验的持久、可重取提案。结果交付与结算在同一数据库事务完成，不依赖客户端 ack；断线后可再次领取同一提案，不二次收费。

用户不满意属于主观结果，不自动无限退款；客户端应用失败属于诊断反馈，复现后或可信服务端校验失败时以唯一 reversal 分录补偿。不得根据可伪造的“应用失败”消息自动无限返还；售前说明结果额度与有限售后重试规则。

取消与成功竞态：事务锁定任务，谁先完成终态转换决定结算。成功已提交则保留结果并结算；取消先提交则不再发起下一次调用，释放额度，已消耗的供应商成本内部计损。

供应商响应未知/超时：除非确认供应商幂等语义，不自动无限重发；标记尝试、受限恢复、记录潜在重复成本。支付退款扣减尚未消费的授权额度，已消费与争议部分进入支持流程，不直接破坏历史账本。

### 9.3 成本指标

记录 provider/model/version、输入输出 token、缓存用量、调用次数、延迟、错误、估算/实际成本与价格表版本。不要把估算成本标作供应商最终账单。

计算：单次有效提案成本 = 全部成功与失败调用成本 / 成功提案数；AI 直接毛利 = 收入减模型及相关直接服务成本。内测到成本不满足预算时降低任务范围、额度或价格策略，禁止通过隐藏费用解决。

## 10. 数据、许可与运营前置

首发推荐一个可用地区的数据平面、一个模型服务和一个支付服务，不能混搭不可用跨境服务。由项目负责人在 W1 确定销售地区、运营主体、模型数据条款、支付开户与税务/退款要求。若面向中国大陆，核查域名备案、相关在线服务义务、模型服务条件和支付接入；不能直接认为沿用模型 API 就自动满足所有义务。

认证默认邮件一次性链接 + HttpOnly/Secure/SameSite cookie；状态变更做来源/CSRF校验。反向代理、限额和日志脱敏都须有测试。不得把模型密钥放入 `VITE_*`、localStorage、客户端日志或工程。

建议原始 prompt/规范化快照/提案保留 7 天供恢复，之后删除；用量及账务元数据按明确政策保存，账务法定期限由运营主体确认，不擅定永久保存全部内容。提示词不进入通用分析事件，不将原始对话用于训练作为默认条件。

保留 MIT 和第三方声明，核验实际资源授权、公开静态资源中哪些可以合法再分发。新品牌、IndexedDB 数据库和部署源独立，不删除上游数据。官网不得把开源编辑功能宣传成独家原创。

## 11. 测试与上线验收

### 11.1 自动化门禁

| 类别 | 门禁 |
|---|---|
| 基线 | `npm test`、`npm run build`；如现有失败，记录与分离，不掩盖 |
| 契约 | JSON schema、未知字段、越界、超大请求、版本不兼容 |
| 范围保护 | camera-only 的受保护字段变化为 0；锁定实体不变 |
| 工程 | 创建/修改/撤销/恢复/刷新后不重复提交 |
| 账本 | 并发扣额不透支、终态不双结算、重复事件不双入账 |
| 任务 | worker 崩溃、租约超时、取消竞态、供应商不确定结果 |
| 授权 | 跨用户任务/订单访问拒绝，账务值由服务端计算 |
| 导出 | 编码能力检查、取消、失败后工程可继续编辑；参考包20项REF用例全部通过 |
| 隐私 | 构建产物无密钥，日志无原始敏感内容，删除按策略执行 |

### 11.2 AI 与浏览器验收

建立 50 条固定评测：20 创建、20 局部修改、10 无效/冲突/恢复案例，覆盖五类套件。使用固定模型版本与温度；固定集与 10 条独立保留用例分开，避免只对提示词调优。

目标（发布门槛，不是当前实测）：有效任务成功率至少 90%，局部保护字段无破坏，失败不污染工程，实际付费账本与订单用例全部通过。画面评分包括人物是否入镜、机位方向、走位、时长、目标动作可辨认；人工复核失败与随机样本，不能只看 JSON 合法率。

浏览器首发验证 Windows 桌面 Chrome/Edge 稳定版、macOS Chrome；锁定测试时实际版本及硬件。目标先保证 720p、24fps、5—15 秒导出；MP4 不可用时提供明确 WebM 回退，不把浏览器下载事件当成文件已存盘。Safari/Mobile 未通过前仅展示有限支持说明。

性能目标：固定低复杂度内置场景预演在约定测试设备上达到 30fps，首个可操作示例加载 p75 小于 8 秒；必须记录网络条件和测量样本。目标未达时限制资源/规格，不虚报普遍性能。

### 11.3 产品门禁

5 名目标创作者中至少 4 名无需开发者代操作完成创建、一次局部修改和工程备份；记录中位时间，目标 10 分钟以内。付费试点建议招募 10 名符合条件的创作者，观察 14 天：是否至少 3 人真实付费，且付费者中至少 2 人用于第二个项目。此为内部决策阈值，小样本不代表市场比例；未达标不自动扩大推广。

## 12. 宣传与分析埋点

主标题：描述你想拍的镜头，AI 帮你排机位和走位。

副标题：生成可编辑的三维预演，局部修改镜头，并将参考视频与对应生成提示词一起准备好。不是最终成片生成服务；未验证的模型入口不标支持。

首发内容：一个完整创建演示、一个“只改机位”的对照演示、一个可下载工程、一个与上游开源版本的透明差异页。宣传功能仅在实现验收后上线，不宣称唯一、零外传、百分百控制或未经测量的节省比例。

事件：landing_view、example_open、login_success、ai_task_requested、proposal_ready、proposal_applied（客户端反馈）、first_export、reference_package_ready（文件生成/下载发起，不代表外部生成成功）、reference_package_failed、project_reused、checkout_started、payment_confirmed（服务端）。分析需固定匿名/登录身份衔接和窗口，不采原始提示词；广告同意要求按地区处理。

先做工作室试点和可复现视频，再做问题型 SEO：人物走位、机位设计、局部修改、白模预演。没有验证的搜索量不作为流量预测。CAC 包括内容与支持投入，不能把自然搜索当作零成本。

## 13. 排期、外部决策与范围变更

REF-01纳入通用参考包：原32项/66人日新增WEB-033—038共9人日，当前38项/75人日（A37/B38），不重复叠加之前6—11人日粗估；AI润色与专用模型验证另计。

默认排期：两名开发者（前端/图形 A、后端/AI B）+ 每周约 2 天产品/测试支持，9 周基础开发 + 2 周风险缓冲；AI 编码加速已包含在估算，不再重复减半。详见[排期表](docs/development-schedule.md)。

W1 必须确定：正式品牌候选、销售地区/主体、模型 provider 及预算上限、支付 provider 与开户、邮件服务、部署地点。未确定时使用 mock/沙箱继续工程，但不声称真实付费已就绪。

架构评审需批准：同源 API、数据库选择、数据保留、首版提案交付计费规则、不自动续费、无云工程。涉及改变这些约束必须更新任务依赖、验收和排期，而不是直接在代码中悄悄扩展。

### 13.1 参考包范围决策

以[详细规格](docs/video-reference-package.md)为接口与验收依据：固定24fps并归一化到帧网格，首尾帧来自实际输出采样，单包只含一个连续镜头；内部节目切换先拒绝并要求选机位或缩范围。通用包不自动发送给外部平台。SD 2.0含义未确认，不自动映射为Seedance；MiniMax等具体模型/网页/API能力必须核验后再添加适配。

## 14. 下一步

计划确认后从 WEB-001 开始；先执行环境/基线检查与接口样例，再并行推进前后端。每个任务以验收证据关闭，不以“代码生成完毕”关闭。完成 WEB-032 前只能称开发版或内测版，不开展公开付费宣传。
