# 开发进度与续接记录

## 最新冻结检查点：RP2 局部 PASS；RP3 首轮返工实施完成，复审因模型额度中断无结论

按负责人要求再次冻结开发，将 RP3 实现与返工测试保存到远程 `checkpoint/dsk-003-progress`。本次父基线为 `cc10acef36f971cfa2ad1ad82bdbd342679c01d1`；新提交号以 Git 历史为准。**不是 RP3 或 DSK-004 验收通过，也不是发布版本。**

### 最新任务状态

| 项目 | 状态 |
|---|---|
| DSK-001～003、SQLite 前置门禁 | 既有技术/独立 PASS 保留 |
| RP1 与 validation-01 | 独立最终 PASS；历史与证据限制保留 |
| RP2 与 ownership-01 | 独立最终 PASS（agent_303386b4，基线 cc10ace）；父任务两轮及 REPLAN 历史不改；N1/N2 非阻断测试覆盖建议保留 |
| RP3 | 已实施 host16 统一 permit；首轮独立审查 REWORK，要求补 R1/R2/R3 覆盖；窄返工完成、service 零业务改动（hash bb7224bd…） |
| RP3 复审 | 编码代理交接完成（RP3 全集21/21、两套件86/86、npm 558/558）；**复审调用因 gpt-6-astra usage_limit_reached 中断，无审查结论** |
| RP4～RP8 | 未开始 |
| DSK-004 整体 | REWORK/未完成 |
| DSK-005～008、016 | 未开始 |

### RP3 当前成果与证据边界

- 已修实际缺陷：`snapshot.read` 缺 host 总检查；仅补 size 会让取消 pending 后 map 清空而无限累积挂起初始化。实现独立身份 `hostPermits` 上限16，两方向第一 await 前同步预留；取消立即离开 map/session 但 permit 持至初始化 settle；ready 交接不双计；迟到 settle 只释放自己身份；sender 销毁无 close 也幂等终结；commit/末块等释放路径身份守卫。
- 首轮红证据：未修快照上 13 用例中 8 个真实行为红（17 下载、16 上传+第 17 下载、15 槽同 tick、取消循环累积等）。
- 返工补齐：同 session 迟到 resolve/reject、reload 新代、跨 frame 身份竞争；16 真实上传满槽后第 17 下载拒绝；fsp.open 尾段覆盖；真实 DB 登记失败释放；A9 三阶段 dispose 含 db.close 恰一次计数。
- 未证实归因如实保留：npm 首轮曾出现两个既有文件失败、隔离复跑通过，具体原因未证实，不声称生产回归；round0 F1 closeParked 的冷负载解释也已降级为未证实。
- R3-a 首次旧快照测试因 harness 未走指定入口误打包现源，已作废并注明；修正后旧码红属 round0 同族已修缺陷，不当作新缺陷。
- 本次复审中断没有产生 PASS/REWORK；不能把它当作任何审查结论。当前无活动编码任务。

### 恢复时从这里继续

1. 核对分支/HEAD/工作树；保留后续改动，不重跑已通过 SQLite 门禁。
2. 读取 `DSK-004-RP3.yaml`、`implementation/DSK-004-RP3.md` 和 `tmp/dsk-004-rp3-coder-20260919T015354Z/`（本机存在；其他机器需在已批范围内复现，不补造日志）。
3. **首先由 director-reviewer 完成 RP3 复审**（上轮审查人 agent_a4dacbb5 的三组覆盖要求），重点：迟到 settle 不清新槽/permit、16 上传满槽拒绝、open 尾段与 DB 失败释放、A9 关库恰一次、两次 npm 既有文件偶发失败的归因是否可能掩盖本任务回归。
4. RP3 PASS 后才进入 RP4；父 DSK-004 仍需 RP4～RP8 及整体审查。原生 UI/Windows11/断电/安装包未验收。
5. provider UUID 为本机映射，额度耗尽时如实报阻塞，不静默换代理代审。

## 以下为上一冻结检查点（历史，状态以本页顶部为准）

### 当时标题：RP1 PASS；RP2 ownership-01 已实施待独立审查

按负责人最新要求暂停开发与审查，将当前源码、测试、批准/返工记录一起保存到远程 `checkpoint/dsk-003-progress`。此检查点父基线为 `99c2582ee50e7abe1caa480628db360dd3fd9e66`；新提交号以Git历史为准。**这不是RP2或DSK-004验收通过，也不是发布版本。**

### 最新任务状态

| 项目 | 状态 |
|---|---|
| DSK-001～003、SQLite前置门禁 | 既有技术/独立PASS保留 |
| RP1与validation-01 | 独立最终PASS；两轮及REPLAN历史、日志缺失限制保留 |
| RP2原任务 | 两次返工后独立最终REPLAN；排空修复成立，但提示/重试归属F3/F4未闭合 |
| RP2-ownership-01 | 获批关联修订已IMPLEMENTED；coder自测通过；**独立审查未完成** |
| RP3～RP8 | 尚未进入实施验收，须前项通过后串行 |
| DSK-004整体 | REWORK/未完成 |
| DSK-005～008、016 | 未开始 |

### 本次保存的成果与证据边界

- RP1：frame generation失效、取消/提交仲裁、已提交回执及清理失败模拟对照已独立认可；旧日志缺失和假阳性修正不被本次提交抹去。
- RP2：已实施确认后停服、pending/后台清理排空、整体真实超时、清理失败台账与非阻塞重试、普通退出/更新前关库及BLOCKED保护。原最终审查仍REPLAN，不以534项历史绿测抵销F3/F4反例。
- ownership-01：main协调器改为非递归pump，提示pending时外部重试不抢占，连续失败的用户retry由循环消费；对应测试与历史证据纠正已写入。这些是**coder交接声明，待独立审查确认**。
- coder最新记录：RP2两套unit共38项、`npm test`537项通过；build/prepare、files（9检查）、storage和exit桌面回归退出0。此次冻结提交没有重新运行这些命令。
- files桌面首次后台执行连接卡住，原空日志保留，精确清理本任务进程后前台复跑通过；不将该失败清理当自然退出证明。Phase C插桩/CDP、Phase D自然退出、dialog stub与原生人工验收边界保持区分。
- 最新审查调用`agent_0437bb4f-cf75-47aa-a0d9-aec340ce30cc`在返回结论前取消；不能把取消解释为PASS或REWORK。当前没有活动编码任务。
- 原始日志/测试库/profile/冻结探针在ignored `tmp/` 等目录，本次不上传。其他机器拉取的是源码与结论记录，不能假称原始证据已同步；本机prepared也不随Git更新。
- 本次不更新默认分支、不发布；不纳入密钥、依赖、缓存、用户工程或运行数据库。

### 恢复时从这里继续

1. 核对检查点分支、HEAD及工作树，保留后续改动；不重新迁入、不重跑已通过SQLite门禁。
2. 读取`.zcode/workflows/tasks/DSK-004-RP2-ownership-01.yaml`、原`DSK-004-RP2.yaml`及`implementation/DSK-004-RP2.md`；RP1保持PASS。
3. **首先调用director-reviewer审查ownership-01实际增量与验收证据**，不要先开启RP3或再写一轮无依据修复。无本机日志时，在已批准范围用新隔离目录复现，不补造旧日志。
4. 重点检查提示pending时close/quit/真实second-instance统一准入，连续两次失败后第二次retry不丢、无递归/双owner、取消/异常锁定、迟到updater回调不撤新授权，普通close成功不误发更新安装许可。
5. 关联独立PASS后再登记RP2局部PASS并推进RP3；父004仍须RP3～RP8及整体审查。原生UI/Windows11/断电/安装包仍未验收。
6. 代理provider UUID是本机连接映射，拉取到其他机器需核对可用绑定；不可静默换代理代审。

## 以下为原续接阶段记录（历史，状态以本页顶部为准）

### RP2第二轮返工后的待审时点

源码根`E:/myProgram/DirectorDesk/director-desk-web`，HEAD `99c2582ee50e7abe1caa480628db360dd3fd9e66`；下方旧机器/旧RP1状态为历史。未提交推送。

- RP1 A1—A4业务修复已经独立确认；关联`DSK-004-RP1-validation-01`经 reviewer `agent_692dd733-70e4-492f-8029-30415a6c073c`最终PASS，V1—V6通过，现登记RP1局部PASS。
- 精确`.part`路径一次性模拟EPERM确实发生于COMMIT之后，成功回执与DB一致；明确模拟I/O，不证明OS文件锁。独立storage51/51退出0，coder定向4/4、npm499/499；本关联未重复build/Electron/gate。
- 原RP1两次返工/REPLAN历史与E01证据限制保留：关联attempt1原始SyntaxError日志被覆盖，仅有coder NOTE，非完整历史；现存成功证据和独立复跑足以支持局部验收，不补造。
- 父DSK004仍REWORK，RP2—RP8尚未通过；RP1PASS不代表004整体或人工UI/断电/安装包验收。
- RP2（批准任务单`DSK-004-RP2.yaml`）第2次审查REWORK仅F1/F2（重试无界await/异常无原生提示），已按具名问题完成修复与全链验证，详见文末"DSK-004 RP2 第2轮返工记录"；IMPLEMENTED待最终独立reviewer（再审不通过转planner），不代表RP2已PASS或父任务通过。


## 当前本机状态：两轮返工未通过，已升级重规划

本轮实际工作目录 `E:/director-desk/director-desk-web`，已快进拉取 `66ce152ccccab068765fdff8f6968e90c81584f9`；下方旧机器路径和暂停说明均为历史。

- 三个 director 代理已实际调用成功，沿用最新拉取的 provider 配置，未修改配置/依赖/锁文件/相邻仓库；本轮未提交或推送。
- DSK-003 与 SQLite gate 的独立 PASS 保留，没有重新迁入或重跑 gate。
- 第1轮 B 段已接续实施，474 项测试及构建/桌面回归通过，但整体独立审查 REWORK，发现 F01—F12。
- 第2轮已实施，独立 `npm test` 489/0/0；5000/10000快照回还、并发rev1视图与rev2保存、64MiB IPC传输得到验证，但**第二轮整体独立审查仍为 REWORK**，不能标004完成。
- 已升级交 director-planner 重规划，不直接启动第三轮 coder；**重规划方案已获负责人批准**（8个串行子任务 RP1—RP8，逐项编码+独立审查；扩围仅限 SceneWorkspace 可回滚切换原语与 updates.cjs 退出等待接线，计划存档 `.zcode/plans/plan-sess_0f45933a-bf18-47df-952f-6fdc936de21f.md`）。RP1 请求失效与取消仲裁为当前执行项；未通过不推进后续任务和005。

剩余阻断：reload不使备份/恢复请求失效；dispose在pending未排空时关DB且退出未等待；下载绕过host16总量；renderer/canonical仍全量生成、部分读取路径无界且缺峰值；同项目重开未更新binding revision；候选失败/补偿/文档回滚/unconfirmed禁止写不完整；dirty确认期间身份/版本变化未完整复查。

独立审查：`agent_726ea8fd-adac-4dfd-8c9d-077e1bfd3ce1`。本机证据：`tmp/dsk-004-review-1789573034052/`、`tmp/dsk-004-review-r2-1789578564212/`；coder写前和验证：`tmp/dsk-004-rework1-b-coder1/`、`tmp/dsk-004-rework2-coder2/`。这些ignored证据不会自动随Git上传。

64MiB成功目前是桌面IPC链路，不等于实际产品保存函数有界生成；原生目录dialog、unknown保存完整UI闭环、主/renderer峰值、Windows11/断电/安装包仍未验收。当前所有A/B/第二轮改动保留，禁止reset/clean。

记录日期：2026-09-16。最新续接已拉取远程 `0c9cb17887eda8a363ae6472d7917bb2a54c8665` 到本机 `E:/myProgram/DirectorDesk/director-desk-web`。下方原检查点内容保留为历史；以本节最新续接结论为准。

## 最新检查点：按负责人要求暂停，保存 DSK-004 返工进度到 GitHub

本次是**含已知缺陷的开发检查点，不是可发布版本或产品验收 PASS**。当前分支 `checkpoint/dsk-003-progress`；本次提交的父基线为 `0c9cb17887eda8a363ae6472d7917bb2a54c8665`，新提交号以 Git 历史为准。

- DSK-001～003 技术 PASS；SQLite 门禁独立 PASS，不再重复迁入或重跑门禁。
- DSK-004 产品第1轮独立审查 REWORK（11项）。A段存储/并发修复已实施，coder记录470项测试和构建/桌面回归通过；**尚未经整体独立复审，不能将coder修复声明当作问题最终关闭**。
- B段派发在工具交接前中断，任务系统无活动任务，未取得B段实施或测试报告；当前旧导入入口无变更、跨工程history.replace路径仍在。按B段未完成续接，不声称已修复。
- 待修 R4/R5/R6/R11：managed新建/导入离开身份、跨工程undo串写、候选切换与启动恢复异步原子性、分页/状态查询及真实UI闭环。其余仍需复审核验，64MiB满额桌面及并发备份等未验收项不关闭。
- 本次提交源码、测试、实施/审查任务记录、计划、进度及获批provider两行修复。排除tmp、profile、依赖、dist/prepared、测试下载资产、数据库及原始日志；这些本机证据不会随Git自动传输。
- provider UUID为本机连接标识，不是凭据；其他机器拉取后须核对自己的provider映射，不能假设本机`6551fef7-…`在其他环境存在。本次不新建provider、不上传密钥。

### 下一次恢复步骤

1. 拉取检查点并记录实际HEAD/工作树；保留后续本地改动，不重做迁入、不重跑已通过的SQLite门禁。
2. 阅读本文件、`implementation/DSK-004.md`、`.zcode/workflows/tasks/DSK-004-product.yaml`、`DSK-004-product-rework-1.md`及批准计划。
3. 只启动一个director-coder接续**同一第1轮返工B段**，不重置返工计数。先核实当前代码，保留A段修改，按R4/R5/R6/R11逐项补反例测试及真实桌面闭环。
4. B段完成后复验A/B全量测试、构建及桌面回归，交director-reviewer独立复审全部问题；未通过不推进005。
5. 忽略目录内旧日志可能只在原机器存在；需要复现时使用新的隔离profile/临时目录，不能伪称已下载历史证据。人工/Windows11/断电/安装包仍未验收。

## A 段交接记录（coder验证，尚待整体复审）

- 首轮独立审查（agent_0f730860-bdfe-4b8d-bce9-278be7441d2e）REWORK，11 项问题见 `.zcode/workflows/tasks/DSK-004-product-rework-1.md`。
- A 段（R1/R2/R3/R7/R8/R9/R10 存储与并发）已修复并以直接反例验证：schema 只读严格识别（trigger/view/列/迁移六变体拒、文件字节不动、初始化失败降级不阻断启动）、同长篡改对象拒、恢复闭包+隐藏行+revision 校验、传输并发同步预留+串行队列+规范 base64、资源预算+流式复制、COMMIT 三分类、续租事务内时钟+受控降级+busy 计数。桌面脚本改为真双活竞争并断言 generation 2/4。
- A 段后全量：`npm test` 470/0 退出 0；tsc/build/prepare 退出 0；contracts/files/storage 三套桌面回归退出 0（storage 曾一次 CDP 瞬态与一次 exit13 悬挂，加 300s 硬超时后复跑通过，已如实记录）。
- B 段未开始：R4 managed 下新建/导入未 leave、R5 跨工程 undo 串写、R6 切换/启动异步非原子、R11 分页与状态查看、真实 UI 闭环。任务状态保持 REWORK。

## 历史记录：DSK-004-product 首轮编码（458 测试，已被返工取代为当前基线）

- coder 按批准计划（ExitPlanMode：storage.v1 契约增量/64MiB/租约/备份恢复/UI 接线）单任务串行实施完成，未重复 SQLite gate。
- 验证（均退出 0）：`npm test` 458/0/0（新增 dsk-storage 19 项、managed-project 11 项；原 428 项未改）、`tsc --noEmit`、`npm run build`、`npm run desktop:prepare`、`test-dsk-contracts-desktop.mjs`（原契约回归不变）、`test-files-desktop.mjs`、新增 `test-dsk-storage-desktop.mjs`（真实 Electron：快照上传/提交/下载/activate 闭环、408KiB 九块跨端规范化 hash 一致、两个真实进程 busy/lease-busy 拒绝、硬杀后 4s TTL 接管 generation=2、释放重获）。
- 单元故障注入覆盖写/sync/close/rename/DB 登记/COMMIT 回执、孤立对象指针不动、outcome-unknown 不盲重试、备份损坏/junction 拒绝、未知 schema 拒写不重建；系统 Node 22.14 与 Electron 内嵌 Node 24.20 的 node:sqlite API 已实测同构。
- 未验收：人工 GUI 闭环（项目库菜单/备份恢复原生对话框/退出重开）、Windows 11 真机、突然断电、安装包、64MiB 满额桌面实测；详见[实施报告](implementation/DSK-004.md)。
- 本轮未 commit/push；provider 两行、计划文件、gate 任务单等先前修改全部保留。

## 历史检查点：SQLite 门禁独立 PASS（本机，未提交）

本轮获批 lf-fix 修订已完成，独立 reviewer `agent_134de967-fc28-4a2e-a3ca-8c6bc186246a` 先预审放行、再审查实际运行后返回 **PASS**，G1—G8 全部通过，无剩余阻断。下方 BLOCKED/REPLAN 段落为保留的历史，不是当前门禁状态。

- 唯一最终运行：`tmp/dsk-004-sqlite-gate/lf-fix-20260916T065222Z-9cd62767-c431-4c8c-a263-88c52ade3065/`。
- 执行前锚定 manifest SHA256：`01fae3476abd58be00921d56c02c505183a15a7fa42439950b088f1f2475f33a`；11个冻结执行文件运行后未变。
- 依次且各一次：真实入口回归exit0（3case/35断言）、异常注入drill exit0（8断言；隔离子fixture预期exit1/FAIL并停止后续case）、正式fixture exit0（5case/34断言）、真实launcher exit0。
- 原始结果、日志与外层回执交叉核实：Electron44.2.0 browser/Node24.20.0/SQLite3.53.4；prepared页面真实加载；文件DB参数化读写、commit/rollback、WAL/连接可见性/checkpoint、跨进程锁忙及释放后重试成立。
- A/B正常退出0后才启动C；C严格只读重开精确5行且实际写入被拒。编排器实际exit0，无超时/强杀。
- reviewer独立重新枚举并计算hash：源码586 tracked+1 untracked共587文件、prepared39文件、五个旧证据目录均匹配执行前记录。管理文档在该次最终审查完成后才更新，不要求它们永久匹配旧运行基线。
- 历史执行脚本不可追溯缺口保留，不追认旧G8；本次PASS仅针对新冻结运行。
- 关联任务单`.zcode/workflows/tasks/DSK-004-sqlite-gate-evidence-replan-01.yaml`已记录PASS。原始探针、日志和数据库在ignored tmp内，不随Git自动上传；不能再次执行或覆盖。

**下一步可进入DSK-004项目库实施，但DSK-004整体尚未完成。** 项目库、项目写入租约、快照、迁移、崩溃恢复、断电耐久性、安装包和人工验收不因门禁通过而视为完成。本轮未commit/push。

## 本机续接历史（保留失败与修订记录）

- 本机最新 prepared 已恢复并独立 PASS：428 项测试、build、prepare、Electron 回环均成功，586 tracked 文件未改；integration SHA256 为 `696284ad06e50e0c4e50cff81263364de3097c3c6c1d499fce387faf462a9281`。
- 原 SQLite 第二轮完整重跑后 G1—G7 及 SQ5 源码保护得到独立认可，但因旧失败脚本版本无法追溯、拒绝重跑分支会覆盖报告，reviewer 返回 **REPLAN**，G8 不放行。
- 已获批准新建 `DSK-004-sqlite-gate-evidence-replan-01`，仅修复证据生命周期；原历史不改，不重置返工计数。
- 新探针冻结 manifest SHA256：`5234a34dcc5fc8cc1a6f3e0a645c90f7990b5d0bd528111f50fc814bcf1b50e8`。执行一次 fixture 后 **BLOCKED**：结果目录未初始化，汇总 `fixture-result.json` 写入时报 ENOENT、退出码 1。五个独立 case 共 34 断言通过，但缺汇总且命令失败，不能视为门禁 PASS。
- **真实 launcher 未执行**。按已批准停止条件封存，不修改冻结脚本、不自动重跑或新建尝试；需要明确批准最小修订后继续。此次是探针编码遗漏，不是 SQLite 功能失败。
- 证据目录：`tmp/dsk-004-sqlite-gate/replan01-20260916T022955Z-edcc3e3b/`；关联任务单：`.zcode/workflows/tasks/DSK-004-sqlite-gate-evidence-replan-01.yaml`。旧失败 attempt2 的四脚本/日志/报告集合摘要前后相同，冻结七脚本未变。
- DSK-004 产品功能及 005—008、016 仍未开始；未 commit/push。

### results-init 修订续接（已封存）

负责人明确继续后，在全新目录补齐fixture的results初始化，未改旧冻结脚本。新manifest SHA256：`0257e6d83df7384fb5866e3a913384de1cc08dd923624d252759f1f96e6a3b3d`。

- fixture一次运行退出0：5个case、34断言及汇总均PASS，原目录遗漏已解决。
- launcher一次运行退出2：`launcher.cjs`取得自己的`launcher.lock`后，仍用包含自身锁的阻断清单复检，必然拒绝；主会话已直接核实代码第67—72行。
- 未启动编排器或Electron，真实G1—G8未执行；这仍是探针启动逻辑缺陷，不是SQLite失败。
- 新目录`tmp/dsk-004-sqlite-gate/replan01-results-init-20260916T033611Z-81032b89-551b-4078-bc08-c8e257f5fda3/`已封存；未修改冻结代码、未重试、未commit/push。
- 后续修订应先覆盖真实launcher首次启动与再次拒绝两条路径的隔离测试，再冻结、放行一次真实门禁，避免只测底层锁helper而遗漏入口逻辑。当前保持BLOCKED。

## 以下为拉取的历史检查点记录

原记录：按负责人当时要求暂停开发，保存成果到远程检查点分支。

**这是进度检查点，不是 DSK-004 完成、DSK-008 交付或发布版本。**

## 当前状态

| 项目 | 状态 | 说明 |
|---|---|---|
| 三个 director 代理 | 实际调用成功 | planner、coder、reviewer 均实际读文件；不据此证明精确后台模型路由 |
| DSK-001 / DSK-002 | 历史技术 PASS | 人工验收仍待办 |
| DSK-003 | **独立技术 PASS** | 两轮返工完成；最终独立测试 428 通过、0 失败、0 跳过，类型检查退出 0 |
| DSK-004 SQLite 门禁 | **REWORK，暂停** | G1—G7 已经独立审查认可；G8 的 SQ5 文件保护清单证据仍待补齐 |
| SQ5 第二轮修复 | 脚本已写，**未运行、未复审** | 停止指令到达后未启动新实验，不能视为通过 |
| DSK-004 项目库、租约、快照、迁移 | 未开始 | SQLite 实验不能代替产品功能 |
| DSK-005—008、016 | 未开始 | 继续按既定依赖顺序推进 |

源码根：`E:/director-desk/director-desk-web`。本轮起点：`checkpoint/dsk-003-progress` / `8b09b43c1fb03c8a42d98cd2e50b64db5486a98e`。本次保存提交号以 Git 历史为准，不在同一提交中自引用。

## DSK-003 已交付与验证

- 修正真实主 frame 身份检查，拒绝同 URL 不同 frame 对象。
- 拒绝重复角色，以及指定镜头数与总时长无法同时满足 5—15 秒/镜头的输入。
- 流程总模型请求预算限制为 0—17；请求相关事件必须携带 requestId 与 stepId。
- 共享 fixture 扩为 151 例（122 schema + 29 IPC），覆盖全部 11 个动作正反例、4/5 镜头、边界值及目标错误断言。
- 执行真实 preload 的 VM 测试验证发送版本；修复七个夹带无关错误的测试反例。
- reviewer `agent_eebea5ee-801a-4005-ad3c-7ed7b08c25c0` 第二轮独立终审 **PASS**：`npm test` 428/0/0，`tsc --noEmit` 退出 0，最终文件 hash 全部匹配。
- 第一轮最终 build、desktop:prepare、真实 Electron 回环均退出 0；第二轮未改生产输入，reviewer 核对产物一致后继承这些证据，未冒称重复运行。

prepared integration SHA-256：`696284ad06e50e0c4e50cff81263364de3097c3c6c1d499fce387faf462a9281`。

**N1 仍是 DSK-008 前契约门禁**：源工程 revision/hash/scope 的完整可信关联、规范化与源变化失效方案尚未实现。若需公共字段或语义变更，必须重新规划并批准。

## DSK-004 SQLite 已证实与剩余问题

### 已证实

锁文件、安装包、实际 Electron 均为 **44.2.0**。实验在真实 `process.type=browser` 主进程执行，没有使用系统 Node 或 `ELECTRON_RUN_AS_NODE` 冒充；内嵌 Node **24.20.0**、SQLite **3.53.4**。

已独立审查认可：

- 包装入口加载未经修改的 prepared 应用，监听到 `director://app/` 加载完成且无主 frame 加载失败。
- 文件数据库参数化写读、提交、回滚、连接重开、WAL、连接间可见性与 checkpoint。
- 两个独立 Electron 进程的写锁竞争，锁忙无部分写入，释放后受控重试成功。
- A/B 正常退出后启动新进程 C，以只读方式验证三行完整数据与回滚行缺失。
- A/B/C 相关 profile 路径隔离；prepared 39 文件内容未变化。

这些结果**不证明断电持久性、ASAR/安装包兼容性或产品级项目租约**。

### SQ5 尚未关闭

第一轮返工的 Git porcelain 正则解析错误，将带状态前缀的伪路径静默丢弃；16 项保护清单仅包含硬编码文件，漏掉 provider、测试、文档和任务单等动态修改文件。没有证据表明这些文件被破坏，但保护证据不完整，所以门禁仍是 **REWORK**，不是 PASS。

第二轮脚本已改为 NUL 分隔枚举、覆盖断言及完整前后 hash，并移除只读重开失败后的普通打开回退；**尚未执行**。提交会改变工作树状态，续接时必须先检查保护清单断言是否适配新的基线，不能机械运行或把未运行代码当已验证。

## 本机证据与续接入口

以下目录被 Git 忽略，**不随本次提交上传**；换机器不能假定存在：

| 路径 | 内容 |
|---|---|
| `artifacts/tmp/dsk-003-resume/` | 依赖恢复和原状态 423 测试、构建、prepare、回环日志 |
| `tmp/dsk-003-rework-1-*` | 第一轮修复、失败及最终成功验证证据 |
| `tmp/dsk-003-rework-2-*` | 第二轮 428 测试、类型检查、最终快照 |
| `tmp/dsk-004-sqlite-gate/20260915T174217-pid341/` | 原始门禁最终实验；早期失败日志曾被工具重置，不能恢复或补造 |
| `tmp/dsk-004-sqlite-gate/20260915T175602-rework1-pid1078/` | 保留的 BLOCKED attempt |
| `tmp/dsk-004-sqlite-gate/20260915T180045-rework1-pid1034/` | 保留的 FAIL attempt |
| `tmp/dsk-004-sqlite-gate/20260915T180125-rework1-pid1017/` | 最近已执行实验，退出 0，但独立审查因 SQ5 判 REWORK |
| `tmp/dsk-004-sqlite-gate/20260915T180721-rework2-pid956/` | SQ5 四份修复脚本已写，未执行 |

恢复顺序：

1. 核实分支、HEAD、工作树及三个代理可用性，保留已有 provider 修正；不要再次迁入源码或修改相邻仓库。
2. 阅读 [SQLite 门禁任务单](.zcode/workflows/tasks/DSK-004-sqlite-gate.yaml)，核实 SQ5 第二轮脚本与新的 Git 基线。若本机临时目录不存在，按任务单由 coder 重建隔离探针，不声称日志已存在。
3. 用唯一新 attempt 保留所有失败证据，完成 G1—G8 和 SQ5 验证，随后交独立 reviewer；第二轮仍不通过则转 planner 重规划。
4. 门禁 PASS 后才细化和实施 DSK-004 项目库与不可变快照，不换驱动或降级 JSON。
5. 后续顺序仍为 004 → 005 → 006 → 007 → 016 → 008；未完成人工验收保持待办。

## 保存边界

- 本轮不重复迁入源码，不修改相邻仓库，不访问真实付费业务资源。
- planner/reviewer 原有 provider 修正保留并随当前工作树保存；这些是开发代理配置，不进入产品 BYOK。
- 提交已审 DSK-003 源码/测试修复、实施与任务记录、本进度文件及配置修正。
- 不上传 `node_modules/`、`dist/`、`.audit/`、`tmp/`、测试下载资产、数据库、profile、凭据或用户工程。
- coder 已响应暂停指令并报告本任务无残留运行进程；本次提交不继续业务开发。
- Windows 11、人工窗口交互、安装包及真实供应商兼容性均未验收。

相关资料：[实施报告](implementation/DSK-003.md) · [续接历史](implementation/PROGRESS.md) · [DSK-003 任务单](.zcode/workflows/tasks/DSK-003.yaml) · [任务总表](任务列表.md)。

## DSK-004 Rework-1 B 段续接记录（本机，2026-09-16，coder 已验证）

基线 `66ce152ccccab068765fdff8f6968e90c81584f9`（A 段 checkpoint 后 clean）。按续接任务单 `DSK-004-product-rework-1-B-resume.yaml` 完成同一轮返工的 R4/R5/R6/R11：managed 切换协议（dirty 确认+autosave 排空+leave，失败保留原状）、项目库打开走 `SceneWorkspace.reset` 阻断跨工程 undo 串写、候选创建/打开改为“activate 成功后再提交”且失败释放候选恢复原 binding、startup 全程 busy+epoch/revision 校验+bootstrap 失败不回旧 recovery、项目库 nextCursor 分页与 project.status 状态面板及不确定保存重开闭环。备份与隔离日志：`tmp/dsk-004-rework1-b-coder1/`。

验证（真实退出码，本机复跑）：`npm test` 0（474 通过/0 失败）；`npx tsc --noEmit` 0；`npm run build` 0；`npm run desktop:prepare` 0；`test-dsk-contracts-desktop.mjs` 0；`test-files-desktop.mjs` 0；`test-dsk-storage-desktop.mjs` 0（阶段 1—5 与新增 Phase 6 真实 UI 全过：startup 自动载入、dirty 确认取消/接受、真实导入先 leave、unmanaged undo 语义、受管重开 undo 无泄漏、59 项分页、状态面板、UI 离开、坏库启动不回 recovery）。

如实保留边界：备份/恢复原生目录对话框与不确定保存端到端触发留人工验收；R8 的 renderer `saveSnapshot` 全量 JSON 编码再验长问题未在 B 段处理（越出本轮允许范围），交整体复审。未 commit/push；B 段与 A 段同属 rework_rounds=1 的一次返工，待独立 reviewer 整体复审 R1—R11，不自行 PASS。

## DSK-004 Rework-2 实施记录（本机，2026-09-16，coder 已验证）

按任务单 `DSK-004-product-rework-2.yaml`（rework_rounds=2）完成 F01-F12 与关键验收。CP0 保全与 10 个具名红测试（全部先红后绿）；CP1 存储可信性/生命周期（F01 约束级 schema 识别、F02 全链 junction 拒绝、F03 current 关系校验、F04 下载 slot/abort 队列化/begin 活性复查/可等待排空、F05 stat+有界读取+实读证据、F06 推导 4MiB manifest 预算、F07 回据推导、F12 requestGone+排空式 dispose）；CP2 renderer（F08 候选/活动分离+补偿+unconfirmed 拒写、F09 同项目会话复用、F10 统一 dirty 确认、F11 行内滚动+真实指针键盘）；CP3 关键证据（64MiB 满额桌面 round-trip+超 1 字节拒绝、5000/10000 备份 round-trip、并发 revision 视图一致性）。

最终全量（真实退出码）：npm test 0（489/0）；tsc 0；build 0；desktop:prepare 0；contracts/files/storage 桌面回归全部 0（storage 15 个 PHASE-OK，日志 `tmp/dsk-004-rework2-coder2/`）。未提交推送；原生目录对话框真实验收与 outcome-unknown 端到端触发保留为人工边界，待独立 reviewer 整体复审，不自评 PASS。

## DSK-004 RP1 实施记录（本机，2026-09-16，coder 已验证，待独立审查）

按批准 RP 计划与任务单 `DSK-004-RP1.yaml` 完成第 1 项（请求 frame generation 失效与取消/提交仲裁）。父任务 DSK-004 仍为 REWORK，本记录只新增 RP1 实际实施说明，RP2—RP8 未实施状态不变。先在未修改实现上跑红 4 个具名反例（reload 后迟到备份发布/恢复登记/复制中途登记/旧代并行发布，退出 1 存证），再修 `desktop/storage/service.cjs`：frameGeneration 代计数与派发令牌捕获、closeFrameSessions/dispose 同步推进代、requestGone 增代判断接入上传/下载/备份/恢复全部复核点（dialog 返回、复制开始/迭代完成、发布前、登记前）、commit 临界区至同步登记零 await 原子仲裁、恢复清理补删空目录壳。integration.cjs 经核实无需改动。对照钉住：已发布备份不被清理、commit 竞态两分支如实。

验证（真实退出码，日志 `tmp/dsk-004-rp1-coder-20260916-183053/`）：红 1→绿 0（6/6）；存储套件 47/47；npm test 0（495/495）；build 0；desktop:prepare 0；桌面脚本第二轮 0（新增 PHASE7：真实 reload 失效旧 session/transfer、.part 清除、新代保存 revision 1；第一轮退出 1 的 .part 全目录断言误伤 Phase5 硬杀残留已如实保留并精确化，该残留记录为 RP2 输入）。原生目录对话框真实 UI 验收留 RP8/人工边界。未 commit/push；RP1 IMPLEMENTED 待独立 reviewer，不代表 DSK-004 整体 PASS。

## DSK-004 RP1 第 2 轮返工记录（本机，2026-09-17，coder 已验证，待独立复审）

独立 reviewer agent_02369570 首轮 REWORK 仅 R01/R02 两项。R01（高）：commit 后收尾 await 期间 reload，旧请求读最新 revision 生成回据并迟到 rename 污染新代——修复为提交后收尾（固定回据 expected+1、取名、rename、回执）整体并入与登记同一同步仲裁区（零 await），清理挂起/失败仍如实报成功；R02（中）：staging mkdir await 期间 reload 未检查即建库复制——修复为 mkdir 后建库/首次复制前复核，取消仅清本次 staging。integration.cjs 无改动；reviewer 要求保持的"rename 已进入后 reload 如实成功""已 commit 清理失败仍成功"由新增 control 显式钉住，未倒退。

反例先红后绿均有真实退出码：修前 service 快照回换实跑 pattern "RP1 R0" → 1（R01/R02 红、control 绿），修后 → 0（4/4）；全套 storage 51/51、npm test 499/499、build/prepare 0、桌面脚本 0（含 PHASE7 真实 reload 失效）。完整命令/UTC/CWD/exit 码、修前后源快照与精确 filediff 在 tmp/dsk-004-rp1-r2-20260917T021249Z/（旧证据目录未覆盖）。更正第 1 轮报告：service diff 实际 +75/-19（非 +94/-20），第一轮桌面日志同时含 FAIL 与 PHASE-OK 须一并引用。父任务 DSK-004 仍 REWORK，RP2—RP8 状态不变；RP1 IMPLEMENTED 待第 2 轮独立审查。

## DSK-004 RP1 第 3 轮窄返工记录（本机，2026-09-17，coder 已验证，待最终复审）

第 2 次独立审查确认 R01/R02 业务缺陷已关闭、A1-A4 通过，仅 V01（有效红测）与 V02（表述）未过。V01：第 2 轮红验证依赖新 DI 参数被修前源码忽略属无效，改为 esbuild 打包层包装 node:fs/promises 的真实 fsp.rm/fsp.mkdir 屏障（旧/新源码均可达、到屏障 assert、红失败于 revision=2/nameOLD 与 copies=1）；红验证以内存 esbuild 打包修前 snapshot（sha256 8b137b73...记录，不回换工作树），当前源码同组具名回归 4/4 绿；清理对照改真实 fsp.rm 异常（.part 持锁 EPERM），rename 对照明确区分项目 rename 与备份发布 rename。V02：service 注释与文档订正为"主进程零 await 事件循环轮次仲裁，非同一 DB 事务/跨进程原子"。第 2 轮旧 red 与 attempt3 日志如实降级为无效历史（原样保留不补造），本轮日志含完整 CMD/UTC/CWD/源 hash/EXIT（tmp/dsk-004-rp1-r3-20260917T024441Z/）。红 1 → 绿 0（4/4）→ storage 51/51 → npm test 499/499，均真实退出码；业务语义未改未重跑 build/桌面。父任务仍 REWORK，RP1 IMPLEMENTED 待最终复审；再不通过转 planner。

## DSK-004 RP1 关联验证记录（DSK-004-RP1-validation-01，本机，2026-09-17，coder 已验证，待独立复审）

RP1 最终复审（agent_321df250-c394-451a-b119-fd1fe13392af）判 REPLAN：唯一阻断为验证问题——清理失败对照假阳性。实测探针（node v22.14.0）：持有 r+ 文件句柄时本机 fsp.rm(force) 仍成功删除文件，旧对照从未 assert rm 实际失败，"真实 EPERM"声明为假阳性（业务 A1-A4 经 reviewer 认可，不重做 service）。按批准最小补正：esbuild fsp wrapper 扩展一次注入——精确 .part 路径返回 SIMULATED 的 Promise 拒绝（EPERM/EACCES、error.simulated=true），其余透传真实 fs，不改全局原生 fsp；清理对照重写为断言注入恰 1 次、路径精确相等、实际抛错 code、注入发生在真实登记之后（谓词在 rm 时刻捕获 db：revision 已=1 且 current=本次 snapshotId）、commit.ok=true 且回据/顶层 rev/快照 rev/ID 与 db.getSnapshot/getProject current 一致；finally 先快照再恢复注入门。R01/R02 屏障用例语义未改；service.cjs 等生产代码零改动（前后 sha256 15f4d748… 一致）。

旧声明订正（旧文件原样保留不补造）：旧红 runner red-runner-r3.cjs 对屏障只记录打印（当时恰 true）并未 assert，"到屏障 assert"仅适用于 tests 具名回归；"内存 esbuild 打包"不准确——esbuild.build 带 outfile 落盘后 require；部分旧日志仅摘要无逐条 hash（full-storage-suite/npm-test 头部无源 hash，source-hashes.txt 仅 2 条）。attempt1 因文档注释内 glob "*/" 误闭合块注释在任何测试前退出 1（就地覆盖未保留，如实注明），修正后重跑。验证（真实退出码，tmp/dsk-004-rp1-validation-01-20260917T075926Z/）：pattern "RP1 R0" → 0（4/4）→ storage 51/51 → npm test 499/499；未 build/prepare/Electron/gate，未 commit/push。父任务仍 REWORK，rework_rounds=2 与 REPLAN 历史永久保留，原 RP1 不提前 PASS；本关联任务 IMPLEMENTED 待独立复审。

## DSK-004 RP2 实施记录（本机，2026-09-17/18，coder 已验证，待独立审查）

按批准任务单`DSK-004-RP2.yaml`完成"确认保存后可等待的存储排空与统一退出"。前次RP2 coder调用在完成五文件实现、两单元测试与首轮红绿验证后被中断（无成功报告）；本会话核实源码mtime早于前会话全部绿日志（当前树=已测绿状态，修前快照sha256 service 15f4d748…/files c16b9bbf…记录在案），在其上仅补齐：`scripts/test-dsk-rp2-exit-desktop.mjs`（新建，真实Electron A6三进程五段：取消留窗+服务可用、保存→排空→关窗→退出且文档真实写盘、app.quit恰一次确认单一流程、真实backup挂起目录选择器→排空真实预算超时→原生受阻提示→取消后窗口解锁+服务如实停止（storage-unavailable）→释放后真实受控重试干净退出）、`.gitignore`精确白名单例外、实现文档与记录。主进程对话框为CDP注入stub（原生框不可CDP驱动），产品接线为真实代码，证据标签严格区分；原生人工交互留RP8/人工边界。

实现要点：files.cjs共享可等待确认（回执仅解析自身id、旧回执拒绝、回执不自行关窗、取消/保存失败窗口与服务保持可用、clean idle也确认、重复确认共享Promise、reload作废）；main.cjs createExitCoordinator（OPEN→CONFIRMING→DRAINING→READY→FINALIZING，close/before-quit同步拦截重入并入单一流程，blocked原生提示+同额预算重试同源工作、取消不假恢复，排空期间锁交互，成功恰一次最终动作，update所有权仅READY后交quitAndInstall）；service.cjs dispose（首个await前同步拒新+推进generation+停sweeper，会话取消/handler计数/脱离map清理与等待期新增清理全排空，独立真实时钟预算，超时/清理失败/关库失败显式blocked且DB保持打开，重复dispose共享Promise、成功恰一次、blocked可控重试）；integration.cjs prepareExit接线+非打包测试预算覆盖；updates.cjs confirmInstall后await prepareUpdateExit，取消/受阻零安装，未改update-host。

验证（真实退出码；红证据前会话`tmp/dsk-004-rp2-coder-20260917T091520Z/`，本会话全量`tmp/dsk-004-rp2-coder-20260918T013213Z/`）：CE1—CE4修前快照红4/4（attempt1 harness失败原样保留注明）→修后单元23/23；npm test 522/522；build/prepare 0；files桌面0（9检查）；storage桌面0（含RP1 PHASE7不回归）；exit桌面0（新prepare产物复跑）。exit脚本开发期4次尝试日志如实保留。未commit/push；RP2 IMPLEMENTED待独立reviewer，父DSK-004仍REWORK，RP3—RP8未实施。

## DSK-004 RP2 第1轮返工记录（本机，2026-09-18，coder 已验证，待第2次独立审查）

首审REWORK（agent_dceb64db，问题单`DSK-004-RP2-rework-1.md`）三条具名问题全部修复：R1 停服后一切失败路径落新BLOCKED态并保持锁定（不回OPEN假可编辑），close/quit/应用重启三种原生受控重试入口，确认前异常仍可用；R2 未解决清理台账替代逐次错误基线——handle.close与tmp rm分阶段真实重试（100ms有界节奏、already-closed容忍），台账非空blocked且DB保持打开，dispose前sweeper失败同样入账，持续失败多轮blocked、解除后真实清理成功才关库；R3 最终动作一次性授权，finalizeClose抛错与updater同步throw/异步error均清授权落BLOCKED（不再OPEN/quitReady混合或永远FINALIZING），update失败后重试降级正常关窗，update-host/src零改动无真实安装。blocked取消失败后解锁属"设计内降级"的首轮结论已撤回更正。

证据（真实退出码，`tmp/dsk-004-rp2-r1-20260918T021504Z/`）：红跑未修源码10具名反例1（R1×3/R2×4/R3×3；midwait首红为探针笔误已修正并存证）；修后单元30/30、npm test 529/529、build/prepare 0（载荷含修复）；files桌面0（9检查）；storage桌面0（RP1不回归）；exit桌面r1失败（Phase D断言机制：app退出后playwright连接拆除不可读process()，原样保留）→r1b改launch时进程exit钩子后0：A1取消留窗可用、A2保存→排空→关窗→退出真实写盘、B恰一次确认、C1真实backup挂起→超时→受阻提示→取消后窗口保持锁定+dsk如实拒绝storage-unavailable、C2 BLOCKED下close即受控重试无二次确认仍锁定、C3释放后真实排空干净退出（一次确认两次受阻提示全程锁定）、D无插桩自然退出exitCode 0+signal null无强杀。等待期清理时序改为排空真实时序并如实注明"同步停服后无全新清理注册点"边界；update关库顺序升级为真实sqlite实测（dbOpen=false于quitAndInstall时刻）；旧反向断言仅作红反例有意义不作通过表述。未commit/push；IMPLEMENTED待第2次独立审查，父DSK-004仍REWORK，RP3未开始。

## DSK-004 RP2 第2轮返工记录（本机，2026-09-18，coder 已验证，待最终独立复审）

第2次审查REWORK仅F1/F2。F1（重试本身无界await）：清理重试改为非阻塞inflight启动——startCleanupRetry单资源跨dispose至多一个并发尝试、100ms启动节奏、排空循环不再内联await重试、整体真实截止覆盖pending/inflight、截止后不发起新尝试、台账/失败/进行中全保留仅真清理出账。F2（异常BLOCKED无原生提示）：prepareExit抛/finalizeClose抛/updater同步throw与异步error一律经landBlockedWithNotice走同一原生受阻提示（可理解原因），noticeInFlight+retrying双守卫单提示单重试owner，提示自身崩溃按取消处理保持锁定不放行不循环，迟到旧updater error被状态/kind门拒绝不撤新授权不重弹，确认前异常仍OPEN可用。updates.cjs本轮零改动。

证据（`tmp/dsk-004-rp2-r2-20260918T031327Z/`）：红①捕获4具名红后测试自身应答无界致OOM（如实记录为harness失败非全部具名红）；红②有界化后完整8红（F1×2预算内未返回、F2×4零提示、迟到error、竞争双owner），失败原因逐条核验——订正2026-09-18：迟到error与竞争双owner两例经日志核实仅失败于"提示未出现"前置断言（属F2零提示缺陷证据，不能表述为已证明撤权/双owner行为）；红③F1新测试在回填r1内联原文的快照上双红且finally释放干净（快照sha256实为acda4adfede8cdeb0c4caf17450bd76589ad50539faa6d380bf7318ae8af3aad，此前记录89a3294b…有误；为回填构造非原样快照，47行精确diff存证）。修后单元35/35、npm test 534/534、build/prepare 0、files桌面0、storage桌面0（RP1不回归）、exit桌面0——A1/A2/B全过，C1-C3受阻提示+窗口全程锁定+受控重试无二次确认（C含终点插桩与CDP触发close仅证接线），D无插桩自然退出exitCode 0+signal null无强杀，C/D证据严格区分。异常提示四类场景为单元级证据+真实main接线，未在桌面逐一复现。未commit/push；IMPLEMENTED待最终独立复审，再审不通过转planner；父DSK-004仍REWORK，RP3未开始。

## DSK-004-RP2-ownership-01 关联任务记录（F3/F4所有权，新唯一coder上下文，2026-09-18，coder已验证，待独立审查）

round2最终复审（agent_ee46ca0e）REPLAN：F3提示等待期second-instance/retryBlockedExit无守卫可启动退出、F4连续2次finalize失败第2个retry因retrying未清被丢。Ethan ExitPlanMode批准最小重规划（planner agent_ebe0f86a PLAN_READY）新建本关联任务，原RP2两轮返工与REPLAN历史登记保留。仅改desktop/main.cjs（createExitCoordinator及second-instance接线归属：删除retrying递归入口，改唯一非递归pump+唯一owned notice+至多一个用户retry决定由循环消费一次；retryBlockedExit/handleClose/handleQuit三重守卫BLOCKED且无pending notice且无活动flow，外部入口只拒不积压；reportFinalActionFailure先验归属，update失败降级普通close；prepareUpdateExit未改，普通close成功不误报安装许可；工厂内绑定exitCoordinator使真实second-instance接线单测可达）与tests/dsk-rp2-exit.test.cjs（DSK_RP2_MAIN_ENTRY快照入口+appListeners捕获真实注册handler+3个有界ownership用例，finally释放屏障）。桌面exit脚本零改动。公共IPC/preload/service/integration/files/updates/update-host/src零改动。

证据（真实退出码，`tmp/dsk-004-rp2-ownership-01-20260918T081633Z/`，COMMANDS.txt含逐命令CMD/UTC/CWD/EXIT与源hash）：未修快照（sha256 160e786d…，真字节快照非回填，PRE-FIX-MAIN-PROVENANCE.txt）红跑→1（24用例21绿+恰3具名红、有界37.2s无OOM：F3#22红于"提示挂起期间drain 2次"越权启动、F3#23红于真实handler未达协调器（快照绑定仅由createWindow设置、单测环境空转，测例如实标注）、F4#24红于第二次继续等待被丢（卡BLOCKED 2/2）——红因均为次数/越权/丢retry，无新版DI漏接型假红）；修后单元38/38、npm test 537/537、build/prepare 0；files桌面attempt1后台执行模式卡死于playwright连接（应用本身正常、日志空白原样保留、按PID精确清理本任务自建进程）→前台复跑0（9检查）；storage桌面0（RP1不回归）；exit桌面0（A1/A2/B/C1-C3/D，C插桩接线与D自然退出区分不变）。E1订正：复算r2 pre-f1-fix快照sha256实为acda4adf…（原89a3294b…有误，回填构造非原样存档），迟到error/竞争双owner两旧红仅失败于"提示未出现"前置断言、不能冒称已证明撤权/双owner——订正就地写入原RP2 yaml/implementation报告/本文件结构化记录，旧日志未动。未commit/push；ownership-01 IMPLEMENTED待独立reviewer，父DSK-004仍REWORK，RP3未启动。

## DSK-004-RP2 / ownership-01 最终通过登记（主会话，2026-09-18）与 RP3 实施记录（本机，2026-09-19，coder 已验证，待独立审查）

主会话登记：DSK-004-RP2 与 DSK-004-RP2-ownership-01 经 agent_303386b4 于 cc10acef 独立审查通过，RP2 局部 PASS（任务单 status: PASS，原生人工 UI/Windows11/断电/安装包验收保留至 RP8/人工边界）；父任务 DSK-004 仍为 REWORK。

按任务单 `DSK-004-RP3.yaml`（批准 RP1-RP8 计划第 RP3 项，planner agent_523d3657 细化）完成"统一 host16 上传/下载及未终结初始化配额"。修前核实三处实际缺陷：snapshot.read 缺 host 总数检查（17 个跨 frame 下载直入 getObject）；仅补 size 不够——pending 取消即删 map/释放 session 而初始化 I/O 在飞，旧实现 map=0 可重入并实测累积 32 并发 getObject；requestGone 迟到分支（sender 销毁无 close）泄漏 map/槽/宿主。实现仅改 `desktop/storage/service.cjs`：独立身份 `hostPermits` Set（上限16，初始化未终结或 ready 任一成立恰占1，handoff 同 permit 不双计），两方向第一 await 前原子检查+map+session+permit 登记；pending 取消立即离开 map/session 但 permit 持至 settle（无 fake 超时清零）；`cancelTransfer`/commit 仲裁/末块改为身份守卫移除（`map.get(id)===transfer`、`session.slot===id`），迟到 settle 只操作自己身份、释放幂等；下载 settle 后 requestGone 分支主动幂等 cancel；commit 释放在 RP1 零 await 仲裁区内同步完成；RP2 排空条件不受 permit 影响；新增 `_internal.hostPermitCount` 只读钩子。公共契约/host16/64MiB/frame方向/session互斥/objects/退出架构/依赖零改动。

红先绿后（证据 `tmp/dsk-004-rp3-coder-20260919T015354Z/`，逐命令 CMD/UTC/CWD/EXIT 见 COMMANDS.txt）：修前快照（sha256 3f5a751f…，兄弟模块为同字节副本仅供 bundling 解析，provenance 存证）红跑 `DSK_RP3_SERVICE_ENTRY` → exit 1，13 用例恰 8 具名红全部红在 RED 断言且计数精确（17下载=17次getObject、16上传+第17下载=9、15槽同tick败者I/O=16、abort pending后map0替换入=17、map0取消循环累积=32、上传pending取消后新上传=17、sweeper过期重入=32、sender销毁僵尸=1），5 兼容对照绿；A9 dispose 兼容用例对快照绿。修后：storage RP3 13/13、A9 1/1、两套件 79/79、npm test 复跑 551/551 全部 exit 0。如实保留：npm 首跑 550/551，唯一失败为既有 RP2 F1 用例（挂起 handle-close 重试）`closeParked≥1` 断言在冷负载下重试启动晚于预算到点的时序边缘——该机制 RP3 零触碰，dispose 套件独立 3×15/15 及 npm 立即复跑均过；按边界未改 RP2 drain/旧断言，留 reviewer 裁量。attempt1/2 红跑日志为快照 bundling 解析失败（非业务红）原样保留注明。未 commit/push；RP3 IMPLEMENTED 待独立 reviewer，父 DSK-004 仍 REWORK，RP4 未启动。

## DSK-004-RP3 第1轮窄返工记录（本机，2026-09-19，coder 已验证，待复审）

审查 agent_a4dacbb5 REWORK 三组覆盖/声明问题（R1 迟到settle证明力不足、R2 A2名称失实+16真实上传方向缺测+旧红getObject9归因错、R3 缺open尾段/DB登记失败/A9 cleanup独立挂起与close计数），未确认生产缺陷。按窄返工原则 **service.cjs 零改动**（前后哈希 bb7224bd 一致），仅改两测试文件与记录；round1 前源快照与哈希存 tmp/dsk-004-rp3-coder-20260919T015354Z/round1/pre-round1/。

补齐内容：R1×4 用例以逐调用独立可释放真实 getObject 屏障+确定性到达信号（无 sleep 猜时序）证明"旧 pending 取消未 settle→同 session 新 transfer 已占 map/槽/permit→旧迟到 resolve/reject 落地"，断言新 map 对象同一性、槽归属、permit 恰降 1、新传输真实走完，并覆盖 reload 新代（generation 断言）与跨 frame 变体——旧码同绿，如实标覆盖性控制。A2 改名 mixed 准确表述；旧红 getObject=9 更正为 mixed 第 9 下载（旧 tmp 日志不改，任务单/报告已纠正）；新增 R2 独立用例：16 真实上传满槽后第 17 下载同步拒且 getObject=0（旧码红=16U 方向有效具名红）。R3 补 open 尾段（真实 fsp.open 门+handle close 计数代理：取消后 permit 撑满、第 17 拒、迟到 handle 真实 close、begin 如实失败、自身 .part 清理；旧码红属 round0 已修"取消删 map 允许重入"同族在 open 尾段的体现并有独立探针佐证，非新生产缺陷，未改生产）、db.commitSnapshot 登记失败（database-locked 区别于 badJSON，map/槽/permit 释放、revision 不动，新旧均绿）、A9 重写三阶段（handler 挂起 blocked→settle 后 detached cleanup 独立挂起仍 blocked/DB 开/close=0→cleanup 落地关库且 db.close 计数恰 1、重复 dispose 仍 1，计数器取代 dbOpen 作 close-once 证据，新旧均绿）。

验证（逐命令 CMD/UTC/CWD/EXIT 见 COMMANDS.txt ROUND1 段）：新/改名用例 9/9、RP3 全集 21/21、两套件 86/86、npm 复跑 558/558 全部 exit 0；快照行为定界如实（R2 旧码红；R1×4/R3-b 旧码绿=覆盖；R3-a 首次快照跑因 buildGatedService 未走快照入口误打包工作树源——结果作废注明，修正入口后旧码红于第 17 准入断言，与探针一致）。npm 首跑 546/558 的两个失败文件（desktop-dev、dsk-rp2-exit）均非本任务改动文件、隔离复跑全绿，成因未证实仅记负载/顺序抖动疑点，不声称生产回归，失败日志原样保留；round0 的 F1 closeParked 未再复现，其成因表述已在报告更正为"未证实/时序不确定"。未 commit/push；RP3 IMPLEMENTED 待复审，父 DSK-004 仍 REWORK，RP4 未启动。
