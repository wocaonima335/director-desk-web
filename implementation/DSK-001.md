# DSK-001 实施报告：源码快照迁入与隔离桌面基线

状态：IMPLEMENTED（第 1 轮 REWORK 修复完成，待 director-reviewer 第 2 轮独立复审；不表示已验收、已提交或已发布）

- 目标仓库 `E:/myProgram/DirectorDesk/director-desk-web`，HEAD 保持基线 `0136ac52780b9a9380c7601d17f6b9bbbc9c20e3`，未 commit/push。
- 上游仓库 `E:/myProgram/DirectorDesk/director-desk`，来源提交 `eee9234ae38ffc61a7d4576298a0f23739474597`（Release 0.4.8），迁移前后 `git status` 均 0 差异。
- 本文件是 DSK-001 的可追踪实施报告与证据索引（自 REWORK 第 1 轮起生效）。第一轮报告与全部原始证据原样保留于 `.zcode/workflows/implementation/`；其中第一轮 `DSK-001.md` 被复审证伪的两处声明（"其余 544 来源字节一致"、"11 项保护 hash 一致"）以本文件的实测修正为准，第一轮文件不再改写。

## 一、REWORK 第 1 轮修正记录

复审 agent 返回 REWORK，核心隔离代码与原 6 保护文件均通过；返工范围为证据与交付一致性三项。

### R1 导入一致性与保护 hash 声明修正

第一轮声明与实测差异：

| 项 | 第一轮声明 | 复审实测 / 本轮终验 |
|---|---|---|
| 548 个导入文件 | "全部逐字节 cmp 一致（0 差异）"、"其余 544 一致" | 542 逐字节相同；2 仅换行差异（`skills/director-desk/scripts/project-tool.mjs`、`src/automation/builtin-skill.json`）；4 内容差异（见下） |
| 11 项保护 hash | "迁移前后一致" | postcheck 时点（2026-09-14T21:27:55-07:00）11 项一致属实；当前为 9 项与基线一致 + 2 项获准更新（`任务列表.md` 进度、`.zcode/workflows/tasks/DSK-001.yaml` 任务单） |

本轮时点终验（2026-09-15T08:40:36Z，`.zcode/workflows/implementation/DSK-001-rework1-import-verify.txt`）：对上游 `git ls-tree -r` 全部条目逐文件 `cmp` 目标工作树，`total=548 identical=542 eol_only=2 content_diff=4 missing=0`；差异文件再用去除 CR 后的 `cmp` 复判换行差异。4 个内容差异全部是计划内变更：

1. `.gitignore`：末尾追加目标仓库例外（第一轮）+ `!/implementation/`（本轮 R3）。
2. `desktop/file-host.cjs`：新增 `defaultFileLocations` 纯函数（隔离修正）。
3. `desktop/files.cjs`：开发版 Projects/Exports 默认目录进入隔离 profile（隔离修正）。
4. `tests/file-host.test.cjs`：新增隔离回归测试。

按要求未为凑 hash 回改任何文件；换行差异的 2 个文件保持导入原样并如实记录。

保护 hash 分类终验（2026-09-15T08:43:00Z，`.zcode/workflows/implementation/DSK-001-rework1-final-verify.txt`）：`PROTECTED_STABLE=9 AUTHORIZED_MUTABLE=2 total=11`。分类约定：原有 6 个工作流文件 + 4 份规划文档要求与基线一致（已验证 9 项一致，含 AGENTS.md、plan.md、任务列表以外的两份规划、三个 agent 定义、工作流 README 与任务模板）；`任务列表.md` 与本任务单是获准的任务进度/任务记录更新，仅记录当前值、不作稳定承诺。本报告与核验文件不与任务单互相 hash，避免不可完成的自引用。

### R2 命令退出码与启动证据补齐

- `npm ci` 未重跑：第一轮日志 `tmp/dsk-001-logs/npm-ci.log` 已含 `EXIT_CODE=0`（389 包锁定安装，Electron 44.2.0）。
- `npm test`、`npm run build`、`npm run desktop:prepare`、desktop:start 取证脚本均重新执行；Git Bash 管道用 `set -o pipefail` + `PIPESTATUS[0]` 取真实进程退出码，日志含命令、CWD、Node/npm 版本、UTC 起止时间与 `EXIT_CODE` 行，存于 `tmp/dsk-001-logs/rework-1/`：

| 命令 | EXIT_CODE | 日志（tmp/dsk-001-logs/rework-1/） | 结果 |
|---|---|---|---|
| `npm test` | 0 | `npm-test.log`（08:35:52Z→08:36:54Z） | 417/417 通过，`# fail 0` |
| `npm run build` | 0 | `npm-run-build.log`（08:37:08Z→08:37:47Z） | tsc --noEmit、Vite、内置技能构建、隐私检查（18 个 allowlist web 文件）通过 |
| `npm run desktop:prepare` | 0 | `desktop-prepare.log`（08:38:01Z→08:38:24Z） | 产出 `.audit/desktop-app` 隔离 payload |
| desktop:start CDP 取证 | 0 | `launch-verify-rework1.out`（08:38:41Z→08:38:58Z） | 见下 |

- 启动证据更名与澄清：`tmp/dsk-001-logs/launch-verify.mjs` 的 `windowShown` 字段已更名 `rendererLoaded`，并新增 `evidenceScope` 注明"仅 CDP renderer 检查：`.application-menu` 存在；不是原生窗口或人眼可见性验证"。第一轮日志 `launch-verify.out` 中的 `windowShown=true` 应读作"renderer 应用菜单已挂载"，不证明原生窗口人眼可见。本轮复跑实测：`rendererLoaded=true`、`director://app/` 标题"光影舞台 · 导演台"、locations 落在 `.local/desktop-dev-profile/DirectorDesk/{Projects,Exports}`、开发模式 `update('check')` 返回"开发预览不执行远程更新"且 `canDownload:false`、`taskkill` 关闭本任务进程树（PID 21972）后无残留 electron 进程。第一轮原始 `desktop-start.raw.log` 已备份为 `desktop-start.raw.log.round1` 后才复跑。

#### 测试资产准备（准确记录）

- 命令（第一轮已执行，本轮未重复下载，日志 `tmp/dsk-001-logs/download-test-models.log`、`download-fbx-fixtures.log`）：
  - `node scripts/download-test-models.mjs`：下载 Khronos glTF-Sample-Assets（固定 revision `90d7ede14c7e280af263824604b427a1ca02cb66`：CesiumMan、RiggedFigure、SheenChair）与 Kenney Furniture Kit zip，共 13 个文件，写入 `test-assets/external/`。
  - `node scripts/download-fbx-fixtures.mjs`：下载 three.js r185 的两个 FBX 样例及许可/上游 README，写入 `test-assets/external/three-fbx/`。
- Kenney zip 解压布局：zip 保存为 `test-assets/external/kenney_furniture-kit.zip`，解压时 zip 根内容直接映射到 `test-assets/external/kenney-furniture/`，使 `Models/GLTF format/`、`Models/FBX format/`、`Models/OBJ format/`、`License.txt` 等位于该目录下；测试按这些相对路径读取（如 `tests/sequence-project.test.ts:54`、`tests/fbx-package.test.ts:16`、`tests/obj-package.test.ts:54`）。
- 固定来源/hash 清单：`test-assets/external/manifest.json`（13 项：url、bytes、sha256，含 `kenney_furniture-kit.zip`）与 `test-assets/external/three-fbx/manifest.json`（r185 tag、sha256、"仅本地 QA、不得随发布"声明）；许可范围见 `test-assets/external/README.md`（CC0/CC-BY，local-test-only，排除于安装包与公开示例）。
- 降级声明："首跑 18 项失败、补资产后全绿"仅为第一轮编码代理报告，原始失败日志未保留、未经独立复核，不作为验收证据；本轮独立证据是上述 EXIT_CODE=0 与 417/417。

### R3 根 implementation 报告与索引

批准约定要求完成时写根目录 `implementation/DSK-编号.md`，第一轮误写入 `.zcode/workflows/implementation/` 且该路径被上游 source-only 白名单忽略。本轮修复：新增本文件；`.gitignore` 末尾追加最小例外 `!/implementation/`（`git check-ignore implementation/DSK-001.md` 验证未被忽略）；未改任何原 workflow 文件，第一轮证据原样保留。

## 二、证据索引（相对仓库根，路径已验证有效）

| 文件 | 内容 |
|---|---|
| `.zcode/workflows/implementation/DSK-001-baseline.txt` | 第一轮：迁移前双仓库状态、11 项保护 hash 基线、Trellis 缺失记录 |
| `.zcode/workflows/implementation/DSK-001-import-manifest.txt` | 上游 `git ls-tree -r` 全量 548 条清单 |
| `.zcode/workflows/implementation/DSK-001-changes.diff` | 本任务相对上游的全部代码差异（4 文件） |
| `.zcode/workflows/implementation/DSK-001-postcheck.txt` | 第一轮：迁移后 11 项 hash 当时一致（PROTECTED_HASHES_IDENTICAL）、上游干净 |
| `.zcode/workflows/implementation/DSK-001.md` | 第一轮报告（保留原样，两处声明已被 R1 修正取代） |
| `.zcode/workflows/implementation/DSK-001-rework1-import-verify.txt` | 本轮：548 条逐文件 cmp 分类终验（542/2/4） |
| `.zcode/workflows/implementation/DSK-001-rework1-final-verify.txt` | 本轮：保护 hash 分类（9+2）、双仓库状态、gitignore 例外验证 |
| `tmp/dsk-001-logs/npm-ci.log` | 第一轮 npm ci，EXIT_CODE=0（未重跑） |
| `tmp/dsk-001-logs/npm-test.log` | 第一轮 npm test（417 通过；无退出码行，以本轮复跑为准） |
| `tmp/dsk-001-logs/launch-verify.out` / `desktop-start.raw.log.round1` | 第一轮启动取证（windowShown 字段按 R2 澄清理解）/ 原始启动日志备份 |
| `tmp/dsk-001-logs/download-test-models.log` / `download-fbx-fixtures.log` | 测试资产准备（第一轮） |
| `tmp/dsk-001-logs/launch-verify.mjs` | 启动取证脚本（本轮更名 rendererLoaded 并加 evidenceScope） |
| `tmp/dsk-001-logs/rework-1/` | 本轮复跑日志：npm-test / npm-run-build / desktop-prepare / launch-verify-rework1（均含 EXIT_CODE 与时间戳） |

## 三、验收标准对应

- **A1**：来源 SHA 与 548 条清单可核对（import-manifest）；终验 542 同 + 2 换行差 + 4 计划内差异、无缺失；上游 status 0 差异；保护文件按分类 9 项不变、2 项授权更新。
- **A2**：开发 profile 与默认目录隔离（`desktop/files.cjs` + `desktop/file-host.cjs`，回归测试在 `tests/file-host.test.cjs`）；启动实测 locations 在 `.local/desktop-dev-profile/` 下；开发模式更新检查被拒、`canDownload:false`；Documents 未被触碰（第一轮记录，本轮复跑 locations 一致）。
- **A3**：npm ci（第一轮 EXIT_CODE=0）、npm test / build / desktop:prepare / desktop:start（本轮复跑均 EXIT_CODE=0，日志见索引）；"实际 Electron 窗口"证据为 CDP renderer 加载 + 进程树管理，原生窗口人眼可见性未验证，见未验收项。

## 四、未验收 / 未验证项

- 人工真机 Electron 窗口交互走查（CDP 仅证实 renderer 加载，未做人眼 UI 走查）。
- Windows 11 行为（当前 Windows 10.0.19045，不能替代 Win11 验收）。
- 打包安装版行为（non-goal，一期后续任务）。
- 第一轮"18 项测试失败"现象（原始日志缺失，仅存降级声明）。

## 五、审查建议重点

1. `desktop/files.cjs`/`file-host.cjs` 隔离分支与打包版等价性（第一轮已通过，本轮未再改动核心代码）。
2. R1 终验方法是否充分（逐文件 cmp + 去 CR 复判；换行差异 2 文件的处理是否接受）。
3. R2 证据链完整性（pipefail/PIPESTATUS 退出码、时间戳、资产 manifest 引用）。
4. R3 例外最小性（仅 `!/implementation/`，未改上游白名单语义）。

环境：Node v22.14.0、npm 10.9.2、Electron 44.2.0、Windows 10.0.19045 x64、git 2.47.1.windows.1。本轮终验时点：2026-09-15T08:43Z 前后（各证据文件头有时间戳）。
