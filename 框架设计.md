# 桌面短剧 Agent 工作台：框架设计

> DESKTOP-04，规划状态，非已实现架构。产品范围见[plan.md](plan.md)，实现任务见[任务列表](任务列表.md)，证据和商业风险见[项目评估](项目评估.md)。

## 0. 10天开发协作与架构冻结

项目负责人 **xueyangfei**（X）；**gpt-6-astra**（G）只负责计划规划、架构与契约设计、独立代码审查；**glm-5.3-flesh**（L）负责编写全部模块、测试和修复。开发角色不等于产品运行时Agent角色，名称不能硬编码到BYOK。

| 边界 | 规划/审查 | 代码写入 | 放行 |
|---|---|---|---|
| shared/contracts、主进程、状态机、快照、恢复、provider | G | L | X |
| renderer UI、主题、模板、参考包、采样和文稿 | G | L | X |
| 测试/fixture、迁移、集成与修复 | G提出验收与复审 | L编写并执行 | X |
| 真实视觉、范围变化与最终合并 | G提供发现，不代决策 | L按批准意见返工 | xueyangfei |

[AGENTS.md](AGENTS.md)规定planner→批准→coder→独立reviewer；同一工作树串行编码。G的审查上下文与规划上下文分离，检查实际差异与初始工作树，不只读取L的自述。G不得直接修复业务或测试代码；只读复验可运行已有获准测试，禁止用复验改源码/快照基准。

L交付base commit、契约版本、文件清单、运行结果和未执行项，G返回PASS/REWORK/REPLAN；两轮连续返工不通过转重规划，X重新确认变化范围。X最终验收，模型PASS不替代真实运行/画面检查。

可用独立只读审查工作树，但不是两条编码分支；共享契约仍由G规划、X批准、L实现。未经授权不提交或推送，保留已有未提交内容与.zcode配置。

当前工作流说明绑定编码模型builtin:zai/GLM-5.3-Flash，与请求名称glm-5.3-flesh不同，未确认映射。文档不修改model字段，不用提示词冒充模型切换；代理不可用则报告阻塞。

D1—D10保持一期功能目标，D7后以修复回归为主。单写入者减少冲突，但不具备双模型并行产能。复用现有代码提速，不关闭sandbox、scope检查、持久恢复或模型预算；阻塞由X决定，不暗中省略门禁。

## 1. 现有代码与改造边界

源码基线 `eee9234`，以下路径相对于原DirectorDesk源码目录。

| 位置 | 已核查能力 | 不应误判 |
|---|---|---|
| `desktop/main.cjs`、`scripts/desktop-dev.mjs` | Electron BrowserWindow、加载director协议页面 | 当前入口不证明需要外部Chrome，也不证明无头渲染 |
| `desktop/integration.cjs` | 主进程AI/MCP通过IPC调用renderer工具 | 不等于多租户云任务服务 |
| `desktop/ai-host.cjs` | 单Agent、讨论/执行、工具循环、部分取消与修复 | 不是固定阶段流水线；无工具响应不代表镜头合格 |
| `desktop/ai-conversation.cjs` | 对话落盘、中断调用标记 | 不等于工程与视频事务恢复 |
| `src/automation/service.ts` | revision、预检、编辑提交、任务和空间工具 | receipts/previews/jobs主要在内存；preview不是画面验收 |
| `src/spatial/report.ts`、`range.ts` | 采样入镜/遮挡、区间检查和限制说明 | 不是逐像素/连续时间/叙事质量保证 |
| `src/export.ts`、`src/exporting/` | 视频编码与批量相关基础 | 工具save-requested不等于文件已写完 |
| `src/production/bundle.ts` | 已保存逐场提示词、素材ZIP、可选视频 | 不是自动同步的逐镜头参考包 |
| `desktop/builder.cjs`、更新模块 | Windows/macOS打包与更新基础 | 当前配置禁用/缺少签名，不等于商业发行完成 |

复用现有编辑器和纯工具契约，在主进程增加协调器和持久事务边界；不先重写Three.js、改React或安装大型工作流框架。

## 2. 分期进程与数据架构

一期只落地Electron renderer/main、SQLite、文件输出与BYOK/mock。没有自建云API、用户账号、许可证或支付依赖。ProviderAdapter保留接口即可，不预创建空server/licensing模块。以下整体图中官方云分支均属二期。

```text
Electron renderer（sandbox + contextIsolation，关闭nodeIntegration）
  任务式UI / 镜头卡片 / Three.js / 编辑执行器 / 编码
                     ↕ 类型化受限preload IPC
Electron main
  WorkflowController / StagePolicy / ProjectStore / ExportWriter
  SQLite任务日志 / 本地快照 / 系统凭据存储
        ↙                                ↘
BYOK适配器（客户自己的密钥）        官方AI网关（二期，HTTPS）
                                    认证/设备/权益/任务/额度/支付
                                    Node+TypeScript+PostgreSQL
                                    官方模型服务（密钥仅服务端）
```

建议目录（尚未创建）：

```text
desktop/workflow/    状态机、阶段审批、检查点、取消协调
desktop/storage/     SQLite适配、不可变快照、导出暂存与确认
desktop/licensing/   二期：设备登录、签名权益与离线缓存
desktop/providers/  一期BYOK；二期增加官方网关适配
src/director-ui/     简单模式、镜头列表、状态与修改入口
src/shot-pipeline/   纯ShotPlan编译、连续性、范围保护、事实提取
src/reference/      文稿、帧采样、manifest
shared/contracts/   DTO、schema、版本、错误码
server/             二期：官方AI、身份、账本、订单与运营接口
```

SQLite只在主进程使用。DSK-004先验证锁定Electron版本下的SQLite绑定/内置驱动、当前Electron运行ABI与事务能力（正式安装产物ABI在二期DSK-032验证），确定一种实现后冻结；不让renderer直接访问数据库。模型请求异步执行，数据库事务不跨网络等待。

SQLite与大文件无法共享一个ACID事务，必须采用意图日志、不可变快照和恢复核对，而不是宣称一次数据库提交保证所有文件同步。

## 3. 工程、镜头与持久模型

### 3.1 文件组织

```text
用户选择的项目目录/
  project.director
  .director-state/
    workspace.sqlite
    snapshots/<content-hash>.json
    pending/
    backups/
  deliveries/<batch-id>/
```

一期由DSK-002确定独立开发代号与userData隔离，并禁用上游自动更新检查；正式品牌/appId/发行更新源由二期DSK-029/020确定。文件中记录schemaVersion；旧工程可导入，未知新版本禁止盲目覆盖。用户要知道`.director-state`含恢复信息，拷贝单个工程只带工程，不必然带完整工作流历史。

### 3.2 本地实体

| 实体 | 字段及约束 |
|---|---|
| workflow | workflowId、documentId、planVersion、status、budget、providerMode |
| shot | shotId、sceneId、order、sourceStateHash、approvedSnapshotHash、status |
| step | stepId、workflowId、shotId、stage、attempt、inputHash、status |
| tool_intent | opId唯一、beforeHash、afterHash、revision、scope、pending/applied/unknown |
| checkpoint | stepId、snapshotHash、schemaVersion、创建时间 |
| approval | 计划/镜头/输出类型、被批准哈希、对象范围、预算、有效状态 |
| artifact | batchId、shotId、snapshotHash、relativePath、hash、字节数、状态 |
| provider_request | requestId、可选云taskId（二期）、输入hash、usage、已知/未知结果 |

人物身份使用稳定roleId，场景中的实体另有entityId。镜头复制不是新建角色身份；同场景共用资源基准，各镜头草稿独立。

## 4. 状态机与审批

```text
DRAFT → PLANNING → PLAN_REVIEW
→ SHOT_DRAFT → STAGING → CHECKING → SHOT_REVIEW
                         ↑      ↓失败且预算允许
                         └ REPAIRING
→ READY_TO_EXPORT → EXPORT_REVIEW → EXPORTING → VERIFYING → COMPLETED
任意执行阶段 → PAUSING → PAUSED / FAILED / CANCELED
```

SHOT_REVIEW是每镜头待确认状态，可在整段预览中批量确认。协调器允许其他草稿在已有计划范围继续生成，但不得越过最终输出审批。单镜头创建使用相同状态机，不另造一套Agent。

两级权限：用户确认的计划范围 + 当前阶段白名单。计划确认授权在草稿空间做相应写入，不等于允许删除源工程或覆盖磁盘文件。计划、人物身份、预算、范围或输出目录变更会使相关批准失效。

模型输出“完成”不触发COMPLETED。只有已批准镜头与实际产物全部核对通过，协调器才能完成批次。

## 5. 结构化Agent与预算

### 5.1 语义与确定性分工

- 模型负责 `StorySpec`、`ShotPlan`、有限修复提案。
- 程序把计划编译成受约束 `EditOperation[]`，并执行检查和资源准备。
- 默认不让模型自由调用全套桌面工具，更不能执行shell/JS、支付、更新或任意URL。
- 模型提出新增人物/资产或改变计划结构时，返回审批请求，不自动扩大范围。

输入限于必要剧情、当前镜头结构化状态、内置资源目录摘录及检查摘要；不上传全项目媒体。实际视觉分析如后续增加，必须单独征得上传图像授权。

### 5.2 固定范围与预算

计划最多2次调用；每镜头最多3次（1初稿+2修复）；5镜头首次工作流最多17次。每次请求还限制输入长度、输出token、超时、操作数（初值40）及可计算时的费用预警。强制执行次数/token/时间上限，金额仅在价格表可信时估算；这不是软件收费规则。模型参数在DSK-002/021实测后冻结。

自动修复即使按多个错误类别分组，也共用每镜头两次总额度，不能每类各两次无限叠加。达到任一限制转人工处理。额外用户修改是新任务和新预算确认。

一期仅BYOK/mock，不在超时后静默换供应商。网络状态未知时记录unknown；若供应商支持请求状态查询则核对，不支持时人工确认后再尝试，不能假设所有BYOK端点支持幂等。二期官方任务才提供自有requestId结果重取。

## 6. 写入事务、暂停与恢复

### 6.1 镜头事务

1. 主进程记录step意图、输入和beforeHash，持久化任务前快照。
2. renderer在草稿/暂存场景执行预检，返回候选状态及afterHash，不覆盖已批准镜头。
3. 主进程验证schema、scope、人物身份和批准范围；写不可变快照临时文件，flush/close后同盘rename。
4. 数据库事务写入checkpoint、当前草稿指针与opId结果；此时持久状态为真源。
5. renderer确认加载该版本；没有确认时保留“需同步”，重启从快照恢复，不重复执行操作。
6. 人工批准后将镜头批准指针更新为该快照；一次撤销恢复任务前指针，不影响其他已批准镜头。

数据库提交前崩溃留下的孤立快照可检查后回收；快照落盘失败不能推进数据库指针；启动时验证被引用快照存在且hash正确。外部修改project.director时检测hash冲突，保留双方副本，不自动回滚用户文件。

### 6.2 暂停/取消

- 暂停：到安全边界停，记录未完成阶段；继续时核对输入与批准状态。
- 取消：终止当前步骤，保留此前已批准内容；已发生供应商调用可能仍有成本。
- 停止Agent必须同时协调renderer job的取消接口和编码器abort；不能只断开模型请求。
- 无法中断的操作标PAUSING，完成或失败后到检查点，不伪报已取消。
- 窗口关闭：有任务时提示暂停并保存或等待；系统强制终止依靠持久核对。
- 系统睡眠/GPU上下文丢失：暂停并标导出项未完成；恢复后从该镜头重新编码，不承诺从半帧继续。

首发需要活动Electron renderer，非无头后台服务。导出时使用适当电源抑制并在结束/错误释放；最小化/睡眠行为以DSK-023实测为准。

## 7. 连续性和检查

场景基准固定，shot N保存角色起止状态和视线目标。承接可以建议从前镜头末状态创建，但必须显式标注“连续”或“时间跳跃”，不能假设所有剪辑都从前一镜头末帧接续。

程序检查：资源存在、数值合法、时长、范围保护、锁定对象、关键时刻入镜、采样遮挡、可疑空间重叠、方向变化。轴线规则提供警告与用户确认，不把所有越轴视为错误。

已知限制：swept AABB不是精确网格碰撞，7×7射线遮挡采样非像素精度；检查未覆盖的区间要标明。视觉审阅关注画面、叙事、表演和镜头节奏，不能被JSON测试替代。

## 8. 参考包与提示词契约

### 8.1 输出与范围

批次包含一个总manifest与逐镜头子目录。每镜头一个连续摄影机，单包选区不得跨内部节目切镜；批次以已批准镜头计划拆分，不新增通用任意视频自动拆镜。

帧网格24fps：startFrame=ceil(start×fps)，endFrameExclusive=floor(end×fps)，处理帧边界浮点容差后frameCount=end-start；实际时长frameCount/fps。空、倒置、超限范围拒绝。

第k帧工程时间为(startFrame+k)/fps，PTS=k/fps；末帧为(endFrameExclusive-1)/fps而非end。首尾PNG捕获同一渲染采样的第一/最后输入画面，不能包含下一镜头或编辑器辅助层。所有文件基于同一冻结快照。

### 8.2 文稿

`ShotFacts`从实际导出快照提取机位、动作区间、人物位置与时长；与范围求交并转换相对时间。范围前已开始动作写“开始时正在…”，不重复起步；缺乏可靠证据不推断步数、情绪或屏幕左右。

成片设定单独保存roleId对应外观、服装、场景材质、风格及声音期望。未知内容不虚构；白模无声不等于外部模型一定能生成声音。

中文规则编译器输出prompt.txt；通用表述不使用未核实模型引用token。人工改写保留底稿与标记；源状态/范围/风格/编译器变化标过期，必须重新生成或人工重新绑定，不能默默覆盖修改。

### 8.3 Manifest

逐镜头字段：format/version、batchId/shotId、sourceSnapshotHash、engine/compiler版本、请求与有效范围、帧率/帧数、机位模式、角色映射、文稿来源与确认状态、实际container/codec、各文件相对路径/MIME/字节数/SHA-256、警告。

manifest不包含自身hash，避免自引用。总清单列每镜头状态、相对目录与子manifest hash。标准包不含本机绝对路径、账号、密钥和无关工程。

### 8.4 文件落盘与恢复

用户通过系统选择器授权目录；主进程记录目录授权，不接受模型给出的任意路径。输出使用新批次目录及 `.partial` 临时文件，逐项写完、关闭、校验后rename，最后发布manifest。Windows占用、权限、磁盘满、重名必须显式处理，默认不覆盖。

已存在产物只有其hash和批准快照匹配时才能跳过；否则请求新目录或明确覆盖授权。批次失败保留已验证项；完整成功要求所有被选项验证通过。日志completed记录写入时若崩溃，启动重算磁盘hash补记，不重复覆盖。网络盘/跨卷原子性不保证，首版支持本机磁盘，其他目标先提示复制已完成包。

### 8.5 REF验收集

| ID | 核查点 |
|---|---|
| REF-01 | 12—20秒导出为192帧、8秒、正确末帧 |
| REF-02 | 非网格边界归一化与浮点容差 |
| REF-03 | 空、倒置、过短/过长范围拒绝 |
| REF-04 | 跨起点动作不重复起步 |
| REF-05 | 范围外事件不写入文稿 |
| REF-06 | 显式机位与节目切换边界 |
| REF-07 | 终点切换不采下一镜头 |
| REF-08 | 相机/人物/灯光/资源变化使导出过期 |
| REF-09 | 播放头等纯UI变化不使文稿过期 |
| REF-10 | 人工修订保留、旧文稿需绑定 |
| REF-11 | 未知外观不虚构 |
| REF-12 | 未知动作/步数保守表述 |
| REF-13 | WebM回退的文件名/MIME/说明一致 |
| REF-14 | 取消、编码失败、磁盘满不误报成功 |
| REF-15 | 文件hash、路径引用和批次清单可核对 |
| REF-16 | 导出同快照，不混入后续修改 |
| REF-17 | 首尾帧与解码视频采用有损容差比较 |
| REF-18 | 重复导出不调用模型/不扣AI额度 |
| REF-19 | 未验证MiniMax/Seedance/SD2.0不标支持 |
| REF-20 | 无密钥/绝对路径，重名/目录授权受控 |

## 9. 一期BYOK与二期官方AI边界

BYOK密钥仅在主进程，通过系统安全存储；不可用则仅内存使用或明确拒绝记住，不能静默明文。BYOK端点限制HTTPS，localhost例外仅由用户明确配置；不让导入工程修改端点。

以下服务端接口只在二期DSK-030/017/018实施，一期不预建。

官方服务端建议Node/TypeScript、schema验证、PostgreSQL账本和短任务队列。只接收最小语义数据，不承接GPU渲染。外部模型凭据不发客户端；设备身份、userId、金额、额度和provider均由服务端验证。

最小接口：设备授权启动/轮询、刷新/撤销、权益查询、AI阶段请求/状态/取消、创建订单、支付回调、删除请求。设备登录使用系统浏览器确认与一次性device code轮询，明确账户名和确认动作；不依赖可被劫持的自定义协议直接传长期token。

请求幂等键为用户+workflowId+shotId+stage+attempt，重复键不同输入409。输出为schema有效的计划/编辑提案，附输入hash与版本；不包含任意可执行脚本。

## 10. 二期预留：软件权益、额度和交易（未定收费标准）

### 10.1 软件权益

二期DSK-029再决定许可、托管AI包、续费及离线政策；原12个月/30天/7天仅候选，不在一期实现。云签名权益含账号、设备、功能、有效期、公钥keyId与缓存截止；签名私钥不在客户端。如采用离线缓存，其期限由二期批准且不延长许可有效期。时间回拨只作重新验证信号，不声称可绝对防破解。

激活设备额度、换机和撤销由服务端管理；客户端本地标志不是权威。权益过期/云服务不可用时保留查看、备份与普通编辑；不得删除或加密扣留项目。

### 10.2 AI额度状态

使用整数额度和不可变账本。每次远程阶段调用预留固定、事先展示的额度；完整工作流显示按剩余最大调用数推算的上限，不用无限按token增长的账单。

`reserved → settled` 在服务端持久保存有效响应的同一事务完成；失败/交付前取消→released。用户在计划确认前的规划阶段也可能计费，界面必须提前说明。自动修复消耗按已批准上限显示，不偷偷超预算。

本机协调器批准范围并不能保护云费用：服务端同时执行用户/工作流预算与并发限制，不能信任客户端上报低用量或完成状态。返回结果可在7天内凭任务ID重取，不重复扣费。

暂停/取消与成功竞态通过服务端终态事务决定；服务端已完成响应但本地失败，不自动无限退款。售后复现产品错误后写唯一补偿分录。供应商未知响应不盲目重试，同一客户端请求不能导致多个无界远程调用。

### 10.3 支付和退款

价格目录服务端计算，回调原始请求体验签，校验商户、金额、币种和唯一事件。前端成功页不能发权益。重复/乱序事件不得重复grant。退款撤销未消费权益/额度并记录补偿，已消费争议交人工处理，不删除历史账本。

## 11. 安全、隐私与发行

- 保持sandbox/contextIsolation，禁nodeIntegration；preload只暴露类型化IPC，验证sender、请求大小、stage和项目权限。
- 一期main是应用内策略边界，校验IPC/路径/模型权限；不是对抗已控制本机的信任根。二期云端独立验证付费操作。
- 默认关闭新增公共/LAN MCP；保留上游高级能力也不得绕过工作流批准直接修改正在锁定的项目。
- 一期构建/启动不调用上游自动更新，不因没有付费模块而暴露任意IPC；正式安装、签名、更新和品牌信息在二期实现，Agent不能操作发行流程。
- 禁止全局关闭TLS校验来解决部署问题。更新只在无活动写入/导出、已保存工程时进行，迁移前备份，可回到兼容版本。
- 二期拟定内容与提案云端默认7天保留，账务元数据按运营主体政策/法定要求保存；本地工程/恢复历史可由客户删除。删除账号不擅删磁盘。
- 诊断上传需明确选择，日志不默认包含剧情、角色外观、密钥、全路径；不把对话用于训练作为默认条款。
- 一期开发启动覆盖集显/独显、窗口缩放和输出；二期再做非管理员安装/更新回退。最低硬件由实测决定。

## 12. 测试责任与实现顺序

一期：DSK-008/009负责语义与保护，010检查，011/023恢复，012—015/022参考包，016本地模型，024本地安全，025开发态性能，028功能UI交接。二期：029决策，030官方服务，017—019账户账务，031云故障验收，020/032打包发行，033公开门禁。

所有跨进程测试需故障注入：renderer提交后未回执、主进程崩溃、SQLite锁/磁盘满、文件已写日志未记、模型响应丢失、睡眠/恢复；支付回调重放仅二期测试。单测通过不能替代实际预演与输出观看。新增API、数据库和目录都是设计，任务验收时补实际版本与实现位置。
