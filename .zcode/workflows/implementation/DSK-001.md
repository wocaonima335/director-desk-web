# DSK-001 实施记录（源码快照迁入与隔离桌面基线）

状态：IMPLEMENTED（编码与本地验证完成，待 director-reviewer 独立审查；不表示已验收或已提交）

## 来源与基线

- 目标仓库 `E:/myProgram/DirectorDesk/director-desk-web`，基线 HEAD `0136ac52780b9a9380c7601d17f6b9bbbc9c20e3`（迁入前后均未变动）。
- 上游仓库 `E:/myProgram/DirectorDesk/director-desk`，来源提交 `eee9234ae38ffc61a7d4576298a0f23739474597`（Release 0.4.8）。
- 导入方式：`git -C <上游> archive eee9234… | tar -x -C <目标>`。只读上游，未添加 remote、未写入上游对象库；目标仓库未 commit。
- 导入规模：548 个 tracked 条目（源码、资源、脚本、测试、package-lock.json、LICENSE）。
- 一致性证据：全部 548 个文件与上游工作树（干净状态，等价于该提交 checkout）逐字节 `cmp` 一致（0 差异）。注：`git hash-object` 直接对工作树哈希会因 autocrlf 与部分上游 blob 自带 CRLF 产生伪差异，已用 `cmp` 逐字节比对取代；详见 `DSK-001-import-manifest.txt`。
- Trellis 检查：两仓库均不存在 `.trellis`，按指示仅记录、不初始化。

## 证据文件（本目录）

| 文件 | 内容 |
|---|---|
| `DSK-001-baseline.txt` | 迁移前双仓库 HEAD、git status、11 个保护文件 sha256、Trellis 缺失记录 |
| `DSK-001-import-manifest.txt` | 上游 `git ls-tree -r` 全量 548 条清单（mode/type/blob SHA/路径） |
| `DSK-001-changes.diff` | 本任务相对上游的全部代码差异（仅 4 个文件，见下） |
| `DSK-001-postcheck.txt` | 迁移后保护 hash 复验（IDENTICAL）、上游干净、目标状态、环境版本 |

命令日志在 `tmp/dsk-001-logs/`（npm-ci.log、npm-test.log、npm-run-build.log、desktop-prepare.log、launch-verify.out、desktop-start.raw.log）。启动取证脚本：`tmp/dsk-001-logs/launch-verify.mjs`。

## 变更清单（相对上游，全部在允许范围内）

1. `.gitignore`：文件末尾追加目标仓库例外（`!/AGENTS.md`、四份规划文档、`!/.zcode/`），防止上游 source-only 白名单隐藏本仓库工作流与规划文件；其余字节与上游一致。
2. `desktop/file-host.cjs`：新增纯函数 `defaultFileLocations({ isPackaged, documents, userData })` 并导出；无 Electron 依赖，便于单测。
3. `desktop/files.cjs`：Projects/Exports 默认目录改用 `defaultFileLocations`。打包版行为不变（Documents/DirectorDesk/…）；开发版默认目录进入隔离 profile（userData）。
4. `tests/file-host.test.cjs`：新增回归测试：开发默认目录必须位于隔离 profile 内、打包默认目录保持 Documents。

其余 544 个导入文件与上游逐字节一致；上游仓库迁移前后 `git status` 均为 0 差异。

## 隔离设计核实（A2）

- 开发 profile：`scripts/desktop-dev.mjs` 注入 `--director-test-profile=<root>/.local/desktop-dev-profile`（已被 .gitignore 忽略），`desktop/main.cjs` 将 userData 指向该目录；更新配置 `updates.json`、文件位置 `file-locations.json`、Cache/blob_storage 等会话数据全部随之隔离。
- Projects/Exports 默认目录：本次修复后开发版落在 `.local/desktop-dev-profile/DirectorDesk/{Projects,Exports}`；真实启动经 CDP 读取 `files('locations')` 证实（见 `launch-verify.out`），且目录真实创建。
- Documents 未被触碰：启动前后 `%USERPROFILE%\Documents\DirectorDesk` 均不存在。
- 更新禁止：开发模式 `mode='development'`；运行时执行 `update('check')` 返回 “开发预览不执行远程更新；请在打包后的软件中检查”，`canDownload:false`；`download`/`install` 仅 `mode==='installed'` 可用；启动自动检查在 development 模式跳过（updates.cjs:41）。单测覆盖于 `tests/updates.test.cjs`（development/portable 永不安装）。

## 验收标准对应

- **A1**：来源 SHA、548 条导入清单、LICENSE/NOTICE 随导入保留（`desktop/licenses/lazy-val.txt`、`src/animation/library/NOTICE.txt`、`skills/director-desk/LICENSE`）；保护文件 11 项 hash 迁移前后一致（`DSK-001-postcheck.txt`: PROTECTED_HASHES_IDENTICAL）；上游 status 0 差异。
  - 追注：`任务列表.md` 在 postcheck 之后按允许范围更新了 DSK-001 行状态（`未开始`→`已实施待审`，单行变更，`git diff` 仅 1 insertion/1 deletion）；这是任务单授权的进度记录，非规划内容覆盖。其余 10 个保护文件 hash 与基线一致。
- **A2**：代码差异 `DSK-001-changes.diff`；自动测试含新增回归用例（417/417 通过）；实际开发启动路径记录 `launch-verify.out`（locations/updateCheck 实测值）。
- **A3**：全部命令真实执行并记录退出码（见下）。

## 检查命令与退出码

| 命令 | 退出码 | 结果 |
|---|---|---|
| `npm ci` | 0 | 389 包锁定安装；Electron 44.2.0 二进制就绪 |
| `npm test` | 0（首次 1） | 417/417 通过。首次失败 18 项均为缺少可选下载的公共测试资产（Kenney CC0 家具、Khronos 样例、three.js FBX；仓库既有 `scripts/download-test-models.mjs`/`download-fbx-fixtures.mjs` 自述 “Explicitly run to acquire public test fixtures”，zip 需按测试预期布局解压为 `test-assets/external/kenney-furniture/`）。完成资产准备后 417/417 通过，非代码问题，与本次改动无关 |
| `npm run build` | 0 | tsc --noEmit、Vite 构建、内置技能构建、隐私检查（18 个 allowlist web 文件）通过 |
| `npm run desktop:prepare` | 0 | 先核实 Chrome `C:\Program Files\Google\Chrome\Application\chrome.exe`；产出 `.audit/desktop-app`（icon.ico、THIRD-PARTY-LICENSES.txt、隔离 payload） |
| `npm run desktop:start` | 0（取证包装器） | 真实 Electron 窗口启动（`npm run desktop:start -- --remote-debugging-port=9223` + CDP 取证）：窗口显示、`director://app/` 加载、locations/updateCheck 实测通过后 `taskkill /PID <树> /T /F` 仅关闭本任务进程树；无残留 electron 进程 |

环境：Node v22.14.0、npm 10.9.2、Electron 44.2.0、Windows 10.0.19045 x64、git 2.47.1.windows.1。

## 未执行/未验收项（留待人工或后续任务）

- 人工真机观察 Electron 窗口交互（代理经 CDP 证实窗口与页面加载，未做人眼 UI 走查）。
- Windows 11 与打包安装版行为（non-goal，一期后续任务）。
- 未 commit/push；目标仓库 HEAD 保持基线不变，全部变更停留在工作树。

## 审查建议重点

1. `desktop/files.cjs` 默认目录改动对打包版等价性的论证（`defaultFileLocations` isPackaged 分支）。
2. `.gitignore` 例外是否恰当（仅追加、不改上游白名单语义）。
3. `scripts/desktop-dev.mjs` 与更新入口未改动是否满足“必要隔离修正”（评估结论：上游已实现 profile 注入与 development 更新封禁，无需改动，运行时证据已记录）。
